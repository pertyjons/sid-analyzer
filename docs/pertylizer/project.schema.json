{
  "$defs": {
    "AllocationMode": {
      "description": "Voice allocation mode.",
      "oneOf": [
        {
          "const": "Polyphonic",
          "description": "Unlimited polyphony (up to max voices).",
          "type": "string"
        },
        {
          "const": "Mono",
          "description": "Monophonic with retriggering.",
          "type": "string"
        },
        {
          "const": "Legato",
          "description": "Monophonic legato (no retrigger on overlapping notes).",
          "type": "string"
        },
        {
          "const": "Unison",
          "description": "Unison - all voices play same note.",
          "type": "string"
        }
      ]
    },
    "ArpMode": {
      "description": "Arpeggiator step order over the held chord.",
      "oneOf": [
        {
          "const": "Up",
          "description": "Ascending pitch order.",
          "type": "string"
        },
        {
          "const": "Down",
          "description": "Descending pitch order.",
          "type": "string"
        },
        {
          "const": "UpDown",
          "description": "Ascending then descending, endpoints not repeated.",
          "type": "string"
        },
        {
          "const": "DownUp",
          "description": "Descending then ascending, endpoints not repeated.",
          "type": "string"
        },
        {
          "const": "AsPlayed",
          "description": "In the order the source notes start (scan order).",
          "type": "string"
        },
        {
          "const": "Random",
          "description": "Deterministic pseudo-random member per step (seeded by step index, so a\ngiven playback position always picks the same note — reproducible\noffline renders).",
          "type": "string"
        },
        {
          "const": "Chord",
          "description": "The whole chord re-triggered every step (octave cycles per step).",
          "type": "string"
        },
        {
          "const": "Custom",
          "description": "A fixed offset loop ([`Arpeggiator::custom`]) applied to the *single*\nsource note's own pitch (offset 0 = the note), tracking a moving melody.\nThe chiptune / SID arp: one moving note, one held gate, pitch cycles.",
          "type": "string"
        }
      ]
    },
    "ArpOffsets": {
      "items": {
        "format": "int8",
        "maximum": 127,
        "minimum": -128,
        "type": "integer"
      },
      "type": "array"
    },
    "ArpRate": {
      "description": "Tempo-synced arpeggiator rate, as a division of the 960-PPQN grid.",
      "oneOf": [
        {
          "const": "Quarter",
          "description": "1/4 note (960 ticks).",
          "type": "string"
        },
        {
          "const": "QuarterDotted",
          "description": "Dotted 1/4 (1440 ticks).",
          "type": "string"
        },
        {
          "const": "QuarterTriplet",
          "description": "1/4 triplet (640 ticks).",
          "type": "string"
        },
        {
          "const": "Eighth",
          "description": "1/8 note (480 ticks).",
          "type": "string"
        },
        {
          "const": "EighthDotted",
          "description": "Dotted 1/8 (720 ticks).",
          "type": "string"
        },
        {
          "const": "EighthTriplet",
          "description": "1/8 triplet (320 ticks).",
          "type": "string"
        },
        {
          "const": "Sixteenth",
          "description": "1/16 note (240 ticks).",
          "type": "string"
        },
        {
          "const": "SixteenthDotted",
          "description": "Dotted 1/16 (360 ticks).",
          "type": "string"
        },
        {
          "const": "SixteenthTriplet",
          "description": "1/16 triplet (160 ticks).",
          "type": "string"
        },
        {
          "const": "ThirtySecond",
          "description": "1/32 note (120 ticks).",
          "type": "string"
        },
        {
          "const": "ThirtySecondDotted",
          "description": "Dotted 1/32 (180 ticks).",
          "type": "string"
        },
        {
          "const": "ThirtySecondTriplet",
          "description": "1/32 triplet (80 ticks).",
          "type": "string"
        },
        {
          "additionalProperties": false,
          "description": "Exact step length in ticks (sub-grid; e.g. 40 = one PAL frame @125 BPM).\nClears the divided-grid floor for chiptune / SID arps. Clamped to ≥1.",
          "properties": {
            "Ticks": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "Ticks"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Frame-/tempo-independent step rate in **millihertz** (integer, so\n`ArpRate` keeps `Eq`/`Hash`): 50_000 = 50 Hz PAL, 60_000 = 60 Hz NTSC.\nThe tick length is tempo-dependent and resolved against the live BPM in\n`Arpeggiator::step_onset` with drift-free absolute-time rounding.",
          "properties": {
            "MilliHz": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "MilliHz"
          ],
          "type": "object"
        }
      ]
    },
    "ArpVelocity": {
      "description": "How the arpeggiator assigns step velocities.",
      "oneOf": [
        {
          "const": "AsPlayed",
          "description": "Each step uses its source note's velocity.",
          "type": "string"
        },
        {
          "const": "RampUp",
          "description": "Crescendo across each cycle (scaled from the source velocity).",
          "type": "string"
        },
        {
          "const": "RampDown",
          "description": "Decrescendo across each cycle.",
          "type": "string"
        }
      ]
    },
    "Arpeggiator": {
      "additionalProperties": false,
      "description": "Arpeggiator (chain stage 2) — the flagship stream generator.\n\nReplaces the source stream: at each step-grid onset it emits one member of\nthe currently *held* chord (every source note sounding at that tick, viewed\nthrough the upstream chain's pitch transforms), and suppresses the source\nchord onsets themselves. Step phase normally counts from the held chord's\nearliest start; `continuous_phase` instead derives it from absolute pattern\ntime. Stateless and deterministic per the rack contract.\n\nTrill / mordent / turn are constrained presets of this generator (NP3/NP6\nexpose them via the per-note ornament menu); they are not separate code.",
      "properties": {
        "continuous_phase": {
          "default": false,
          "description": "Keep the step cycle aligned to absolute pattern time instead of\nrestarting it at every new chord block.",
          "type": "boolean"
        },
        "custom": {
          "$ref": "#/$defs/ArpOffsets",
          "default": [],
          "description": "Semitone offsets cycled when `mode == Custom`, relative to the source\nnote's own pitch (offset 0 = the note). `octaves` multiplies the cycle:\n`[0,4,7]` with `octaves == 2` plays `0,4,7, 12,16,19`. Ignored by the\nother modes."
        },
        "gate": {
          "$ref": "#/$defs/NormalizedValue",
          "default": 0.75,
          "description": "Step gate as a fraction of the step length (≥1 tick). A 1.0 gate on\nrepeated pitches ties via the engine's boundary coalesce."
        },
        "latch": {
          "default": false,
          "description": "Hold the last chord through gaps (keeps arpeggiating after the source\nnotes end, using the chord that sounded just before the gap).",
          "type": "boolean"
        },
        "legato": {
          "default": false,
          "description": "Hold one envelope across the whole figure (chip-style): each step is\nstamped `legato`, so the engine glides between steps instead of\nre-gating (one attack, pitch cycles). Needs a `gate` near 1.0 so steps\nmeet end-to-end — a short gate leaves a hole the legato join can't span.",
          "type": "boolean"
        },
        "mode": {
          "$ref": "#/$defs/ArpMode",
          "default": "Up",
          "description": "Step order over the held chord."
        },
        "octaves": {
          "default": 1,
          "description": "Octave range (1–4; values outside are clamped at use).",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "rate": {
          "$ref": "#/$defs/ArpRate",
          "default": "Sixteenth",
          "description": "Tempo-synced step length."
        },
        "swing": {
          "$ref": "#/$defs/NormalizedValue",
          "default": 0.0,
          "description": "Swing: odd steps are delayed by `swing × step/2`."
        },
        "velocity": {
          "$ref": "#/$defs/ArpVelocity",
          "default": "AsPlayed",
          "description": "Step velocity behavior."
        }
      },
      "type": "object"
    },
    "ArrangementSection": {
      "description": "A named interval describing the high-level form of the arrangement.",
      "properties": {
        "color": {
          "$ref": "#/$defs/SectionColor",
          "description": "Display color."
        },
        "id": {
          "$ref": "#/$defs/SectionId",
          "description": "Stable section identifier."
        },
        "kind": {
          "$ref": "#/$defs/SectionKind",
          "description": "Semantic role used for presets and future form-aware features."
        },
        "length": {
          "$ref": "#/$defs/Duration",
          "description": "Section duration."
        },
        "name": {
          "description": "User-facing label, independent of the semantic kind.",
          "type": "string"
        },
        "start": {
          "$ref": "#/$defs/Tick",
          "description": "Start position on the song timeline."
        }
      },
      "required": [
        "id",
        "name",
        "kind",
        "start",
        "length",
        "color"
      ],
      "type": "object"
    },
    "AudioTapNode": {
      "description": "A grid-native pre-fader level tap (source only; one-block latency).",
      "properties": {
        "source": {
          "$ref": "#/$defs/AudioTapSource"
        }
      },
      "required": [
        "source"
      ],
      "type": "object"
    },
    "AudioTapSource": {
      "description": "What a [`AudioTapNode`] reads a pre-fader level from.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "A sequencer track's pre-fader instrument output.",
          "properties": {
            "Track": {
              "$ref": "#/$defs/TrackId"
            }
          },
          "required": [
            "Track"
          ],
          "type": "object"
        },
        {
          "const": "Master",
          "description": "The master bus.",
          "type": "string"
        }
      ]
    },
    "Author": {
      "description": "Author information embedded in patch, project, and preset files.",
      "examples": [
        {
          "email": "",
          "license": "CC BY 4.0",
          "name": "Per Jonsson",
          "website": ""
        }
      ],
      "properties": {
        "email": {
          "description": "Email address.",
          "type": "string"
        },
        "license": {
          "description": "License (e.g. \"CC BY 4.0\", \"All rights reserved\").",
          "type": "string"
        },
        "name": {
          "description": "Full name.",
          "type": "string"
        },
        "website": {
          "description": "Website URL.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "AutoInstrumentParam": {
      "description": "Automatable instrument parameters for sequencer automation lanes.\n\nThese are parameter identifiers (no values) used in automation.\nFor engine commands with values, see `engine::commands::InstrumentParam`.",
      "enum": [
        "Volume",
        "Pan",
        "FilterCutoff",
        "FilterResonance",
        "Attack",
        "Decay",
        "Sustain",
        "Release"
      ],
      "type": "string"
    },
    "AutomationLane": {
      "description": "An automation lane controlling a specific parameter.",
      "properties": {
        "points": {
          "description": "Automation points, sorted by tick.",
          "items": {
            "$ref": "#/$defs/AutomationPoint"
          },
          "type": "array"
        },
        "target": {
          "$ref": "#/$defs/AutomationTarget",
          "description": "The parameter being automated."
        }
      },
      "required": [
        "target",
        "points"
      ],
      "type": "object"
    },
    "AutomationPoint": {
      "description": "A single automation point.",
      "properties": {
        "curve": {
          "$ref": "#/$defs/CurveType",
          "description": "Curve type for interpolation to next point."
        },
        "tick": {
          "$ref": "#/$defs/PatternTick",
          "description": "Position within the pattern."
        },
        "value": {
          "$ref": "#/$defs/NormalizedValue",
          "description": "Normalized value (0.0 - 1.0)."
        }
      },
      "required": [
        "tick",
        "value",
        "curve"
      ],
      "type": "object"
    },
    "AutomationTarget": {
      "description": "Target for automation.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "Instrument parameter.",
          "properties": {
            "Instrument": {
              "properties": {
                "instrument": {
                  "$ref": "#/$defs/InstrumentId"
                },
                "param": {
                  "$ref": "#/$defs/AutoInstrumentParam"
                }
              },
              "required": [
                "instrument",
                "param"
              ],
              "type": "object"
            }
          },
          "required": [
            "Instrument"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Track parameter.\n\n`track: None` means \"the track hosting the placement\": the lane follows\nwhatever track its pattern is placed on, resolved by the sequencer\nduring the placement walk. This is the default authoring form — a\npooled pattern should not hard-code a track. `Some(id)` deliberately\nautomates a specific track regardless of where the pattern is placed\n(cross-track automation, e.g. song-spanning fades hosted on a\ndedicated automation track).",
          "properties": {
            "Track": {
              "properties": {
                "param": {
                  "$ref": "#/$defs/TrackParam"
                },
                "track": {
                  "anyOf": [
                    {
                      "$ref": "#/$defs/TrackId"
                    },
                    {
                      "type": "null"
                    }
                  ]
                }
              },
              "required": [
                "param"
              ],
              "type": "object"
            }
          },
          "required": [
            "Track"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Global parameter.",
          "properties": {
            "Global": {
              "$ref": "#/$defs/GlobalParam"
            }
          },
          "required": [
            "Global"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A parameter on a specific module within an instrument's graph (the\ngeneric A2 target — any continuous, RT-safe module parameter).\n\nModule identity is **positional** for this first cut: `module_type` +\n`instance` mirror the structure of `synth_engine::ModuleId` (a stable,\nnon-positional `ModuleId` is a deferred TODO). `synth_sequencer` cannot\ndepend on `synth_engine`, so the engine reconstructs the `ModuleId` from\nthese two fields when dispatching.\n\nThe parameter is identified by its descriptor `type_id` (e.g. `\"cutoff\"`)\nrather than a [`synth_core::Param`] value: `Param` carries an `f32` and so\nis neither `Eq` nor `Hash`, but `AutomationTarget` is used as a map key.\nThe dispatch resolves the descriptor by `param_id`, denormalizes the\n`0..1` lane value through its range, and rebuilds the concrete `Param`\nwith `Param::with_f32`.",
          "properties": {
            "Module": {
              "properties": {
                "instance": {
                  "description": "1-based instance index within the module type (as in `ModuleId`).",
                  "format": "uint16",
                  "maximum": 65535,
                  "minimum": 0,
                  "type": "integer"
                },
                "instrument": {
                  "$ref": "#/$defs/InstrumentId",
                  "description": "Instrument owning the module."
                },
                "module_type": {
                  "$ref": "#/$defs/ModuleType",
                  "description": "Module type (with `instance`, the positional module identity)."
                },
                "param_id": {
                  "$ref": "#/$defs/ParamId",
                  "description": "Descriptor `type_id` of the target parameter (e.g. `\"cutoff\"`).\n`Arc`-interned so cloning this target is RT-safe (see [`ParamId`])."
                }
              },
              "required": [
                "instrument",
                "module_type",
                "instance",
                "param_id"
              ],
              "type": "object"
            }
          },
          "required": [
            "Module"
          ],
          "type": "object"
        }
      ]
    },
    "BipolarValue": {
      "description": "A value normalized to the range [-1.0, 1.0].\n\nUsed for:\n- Audio samples\n- LFO output\n- Modulation amounts\n- Pan position\n- Any bipolar signal",
      "format": "float",
      "type": "number"
    },
    "Bpm": {
      "description": "Tempo in beats per minute (BPM).\n\nStandard range is 20-300 BPM for most music.",
      "format": "float",
      "type": "number"
    },
    "CanvasSize": {
      "description": "Canvas size hint for the rack view.\n\nSerializes as `{\"width\": ..., \"height\": ...}`.",
      "properties": {
        "height": {
          "format": "float",
          "type": "number"
        },
        "width": {
          "format": "float",
          "type": "number"
        }
      },
      "required": [
        "width",
        "height"
      ],
      "type": "object"
    },
    "Chord": {
      "additionalProperties": false,
      "description": "Chord generator (chain stage 1) — expand one source note into a chord.\n\nEach source-note onset is **replaced** by the pitches at the source pitch\nplus each stored interval (semitones; `0` keeps the root, negatives voice\nbelow it). The classic chain is `chord → arp`: the arpeggiator reads the\ngenerated tones through the widened held-notes seam ([`NoteProcessor::\nexpand_pitch`]), so one source note + chord + arp arpeggiates the full\nchord. Intervals are stored inline (no heap) and capped at\n[`MAX_CHORD_INTERVALS`].\n\n**Strum:** with `strum > 0` the tones are struck one at a time, `strum`\nticks apart, ordered by `direction` (a block chord at `strum = 0`). Strum\nshapes *onset timing*, so it only affects a standalone chord — a downstream\narp re-times everything and ignores it. A strummed chord *replaces* the\nsource stream (it scans the source notes by onset, like the arp), so — like\nthe arp — it bypasses per-note trigger probability; the block path respects\nit (it transforms the already-gated buffer).",
      "properties": {
        "count": {
          "default": 3,
          "description": "Number of live intervals (clamped to [`MAX_CHORD_INTERVALS`]).",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "direction": {
          "$ref": "#/$defs/StrumDirection",
          "default": "Up",
          "description": "Tone order when strumming."
        },
        "intervals": {
          "default": [
            0,
            4,
            7,
            0,
            0,
            0,
            0,
            0
          ],
          "description": "Semitone offsets from the source pitch; only the first `count` are live.",
          "items": {
            "format": "int8",
            "maximum": 127,
            "minimum": -128,
            "type": "integer"
          },
          "maxItems": 8,
          "minItems": 8,
          "type": "array"
        },
        "strum": {
          "$ref": "#/$defs/Duration",
          "default": 0,
          "description": "Onset spread between adjacent tones (`0` = a simultaneous block chord)."
        }
      },
      "type": "object"
    },
    "CombineMode": {
      "description": "How a [`ModTarget`] contribution composes with the target's current value\n(base value or authored lane value). Additive-only for now.",
      "oneOf": [
        {
          "const": "Add",
          "description": "`result = current + amount * source`, clamped to the target's range.",
          "type": "string"
        }
      ]
    },
    "ConnectionState": {
      "description": "Connection between two modules.",
      "examples": [
        {
          "from": [
            "osc-1",
            "out"
          ],
          "to": [
            "flt-1",
            "in"
          ]
        }
      ],
      "properties": {
        "from": {
          "description": "Source (module_id string like \"osc-1\", port_name).",
          "maxItems": 2,
          "minItems": 2,
          "prefixItems": [
            {
              "type": "string"
            },
            {
              "type": "string"
            }
          ],
          "type": "array"
        },
        "to": {
          "description": "Destination (module_id string, port_name).",
          "maxItems": 2,
          "minItems": 2,
          "prefixItems": [
            {
              "type": "string"
            },
            {
              "type": "string"
            }
          ],
          "type": "array"
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "CurveStrength": {
      "description": "Strength of an exponential automation curve (`-127..=127`).",
      "format": "int8",
      "maximum": 127,
      "minimum": -127,
      "type": "integer"
    },
    "CurveType": {
      "description": "Interpolation curve type between automation points.",
      "oneOf": [
        {
          "const": "Linear",
          "description": "Linear interpolation to next point.",
          "type": "string"
        },
        {
          "const": "Step",
          "description": "Step (hold value until next point).",
          "type": "string"
        },
        {
          "additionalProperties": false,
          "description": "Exponential curve (parameter indicates strength, -127 to 127).",
          "properties": {
            "Exponential": {
              "$ref": "#/$defs/CurveStrength"
            }
          },
          "required": [
            "Exponential"
          ],
          "type": "object"
        },
        {
          "const": "SCurve",
          "description": "S-curve (smoothstep).",
          "type": "string"
        }
      ]
    },
    "Duration": {
      "description": "Duration in ticks.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "EnvelopeTrigger": {
      "description": "What retriggers a [`NoteEnvelope`].",
      "oneOf": [
        {
          "const": "SourceOnset",
          "description": "Raw source-note onsets — a bounded, pure scan of the pattern notes\n(cheap; the default and back-compatible behavior).",
          "type": "string"
        },
        {
          "const": "StreamOnset",
          "description": "Onsets in the *transformed* terminal stream (e.g. one retrigger per\narpeggiator step). Evaluated by a bounded backward look-back through the\nspine (`NoteGraph::eval_prefix_at_tick`) — more expensive than\n`SourceOnset` (each probe re-runs the spine), so it is opt-in **and its\nlook-back is capped at 1 beat** (`MAX_ENV_STREAM_WINDOW`): more than a\nbeat after the last transformed onset the level reads `0.0`, even when\n`attack + decay` is longer. Retriggers on **dry** transformed onsets\nonly — a delay's echoes do not retrigger it (RT protection: probing\nechoes would compound each in-spine delay's own look-back into an\naudio-thread stall). See `note-grid.md` §11.",
          "type": "string"
        }
      ]
    },
    "EuclideanGenerator": {
      "description": "A source-independent generator that emits a note on each Euclidean onset —\n`pulses` hits distributed as evenly as possible over `steps`, offset by\n`rotation`, one step every `step_len` ticks. Pure tick arithmetic, no state.",
      "properties": {
        "pitch": {
          "$ref": "#/$defs/Pitch",
          "default": 60,
          "description": "Pitch of each emitted hit."
        },
        "pulses": {
          "default": 4,
          "description": "Onsets distributed over the steps (clamped to `steps`).",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "rotation": {
          "default": 0,
          "description": "Rotation of the onset pattern, in steps.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "step_len": {
          "$ref": "#/$defs/Duration",
          "default": 240,
          "description": "Ticks per step."
        },
        "steps": {
          "default": 16,
          "description": "Total steps in the cycle (0 = silent).",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "velocity": {
          "$ref": "#/$defs/Velocity",
          "default": 0.6299212574958801,
          "description": "Velocity of each emitted hit."
        }
      },
      "type": "object"
    },
    "ExposedPortState": {
      "description": "A port exposed on a group boundary.",
      "properties": {
        "label": {
          "type": "string"
        },
        "module_id": {
          "description": "Module ID string (e.g., \"osc-1\").",
          "type": "string"
        },
        "port": {
          "description": "Port name string (e.g., \"out\", \"cutoff\").",
          "type": "string"
        }
      },
      "required": [
        "label",
        "module_id",
        "port"
      ],
      "type": "object"
    },
    "Gain": {
      "description": "Linear gain factor (amplitude multiplier).\n\nA value of 1.0 means unity gain (no change).\nValues > 1.0 amplify, values < 1.0 attenuate.",
      "format": "float",
      "type": "number"
    },
    "Glide": {
      "description": "Per-note glide (portamento / glissando) — taxonomy primitive 2.\n\nDrives the engine's existing `GlideState` (exponential portamento) from\nper-note sequencer data. `Copy`/alloc-free so it can ride the audio-thread\n`SequencerEvent::NoteOn` without heap allocation.",
      "properties": {
        "from": {
          "$ref": "#/$defs/GlideFrom",
          "description": "Where the glide starts from."
        },
        "interp": {
          "$ref": "#/$defs/GlideInterp",
          "description": "Continuous (portamento) vs stepped (glissando)."
        },
        "time": {
          "$ref": "#/$defs/Milliseconds",
          "description": "Glide time (how long to reach this note's pitch)."
        }
      },
      "required": [
        "from",
        "time",
        "interp"
      ],
      "type": "object"
    },
    "GlideFrom": {
      "description": "Where a per-note glide starts from.\n\nEither a signed semitone offset relative to the note's own pitch (the common\ncase) or an absolute source pitch.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "Signed semitone offset from this note's pitch (e.g. `-12.0` = an octave below).",
          "properties": {
            "Semitones": {
              "$ref": "#/$defs/Semitones"
            }
          },
          "required": [
            "Semitones"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Absolute source pitch to glide from.",
          "properties": {
            "Pitch": {
              "$ref": "#/$defs/Pitch"
            }
          },
          "required": [
            "Pitch"
          ],
          "type": "object"
        }
      ]
    },
    "GlideInterp": {
      "description": "Interpolation type for a per-note glide.\n\nThe single taxonomy axis (roadmap: expression primitive 2) that separates a\ncontinuous portamento from a stepped glissando. `Stepped` quantizes the glide\ntrajectory to semitones — the intentional-holds case, where smoothing would be\nthe bug (the inverse of the zipper-noise pitfall).",
      "oneOf": [
        {
          "const": "Continuous",
          "description": "Continuous portamento (smooth pitch ramp).",
          "type": "string"
        },
        {
          "const": "Stepped",
          "description": "Stepped glissando (quantized to semitones).",
          "type": "string"
        }
      ]
    },
    "GlobalParam": {
      "description": "Automatable global parameters.\n\nTempo and swing are deliberately **not** here: both alter the playback time\ngrid itself rather than a per-block value. Tempo lives in the song's\ndedicated tempo map (`Song::set_tempo_at` / `tempo_at`) — the single source\nof truth that drives the sequencer's tick rate. Swing is a note-timing edit\n(`Pattern::apply_swing`), not a continuously-modulatable scalar; a generic\nautomation lane for either would be a silent second source of truth.",
      "enum": [
        "MasterVolume"
      ],
      "type": "string"
    },
    "GlobalProjectState": {
      "description": "Global project state that isn't per-instrument.",
      "properties": {
        "glide_time": {
          "default": 0.0,
          "description": "Glide/portamento time in seconds.",
          "format": "float",
          "type": "number"
        },
        "master_effects": {
          "description": "Master-bus effect chain (the final chain applied to the full mix), in\nprocessing order. Engine-side runtime state, captured here for save/load.",
          "items": {
            "$ref": "#/$defs/ModuleState"
          },
          "type": "array"
        },
        "master_volume": {
          "default": 0.800000011920929,
          "description": "Master volume (0.0–1.0).",
          "format": "float",
          "type": "number"
        },
        "octave_offset": {
          "default": 0,
          "description": "Keyboard octave offset.",
          "format": "int32",
          "type": "integer"
        },
        "return_bus_effects": {
          "description": "Effect chains on return busses (the busses themselves — id/name/fader —\nlive in the `Song`; only the engine-side effect chain is captured here).",
          "items": {
            "$ref": "#/$defs/ReturnBusEffectsState"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "GroupId": {
      "description": "Unique ID for a group within a patch.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "Hertz": {
      "description": "Frequency in Hertz.\n\nRepresents oscillation rate - the number of cycles per second.\n\n# Valid Range\nTypically 0.0 to Nyquist (sample_rate / 2), but audio frequencies\nare usually 20 Hz to 20,000 Hz.",
      "format": "float",
      "type": "number"
    },
    "Humanize": {
      "additionalProperties": false,
      "description": "Humanize (chain stage 4, last) — bounded, **seeded** random perturbation of\nthe final note stream, so a sequenced part loses its machine-uniformity\nwithout losing reproducibility.\n\nA pure buf transform on whatever the rack produced (arp steps, chord tones,\nplain notes). The per-note random is a deterministic hash of\n*(seed, tick, pitch, buffer index)* — never `Math.random` on the audio\nthread — so a given `seed` renders byte-identical every time (offline and\nlive), and changing `seed` rerolls the feel.\n\n**Scope:** velocity and gate (note-length) only. *Timing* humanize is\ndeliberately not here — shifting a note's onset is a multi-tick / engine\nmicro-timing concern (a stage-4 buf transform can only see notes already\nlanded on the current tick, and the per-tick model cannot *rush* a note\nearlier than where the chain placed it). *Micro-pitch* needs a sub-semitone\ndetune field on the emitted note. Both are tracked as follow-ups.",
      "properties": {
        "gate": {
          "$ref": "#/$defs/NormalizedValue",
          "default": 0.0,
          "description": "Max note-length deviation as a ± fraction of each note's duration\n(0 = off; notes with no explicit duration are untouched)."
        },
        "seed": {
          "default": 0,
          "description": "PRNG seed for this processor. Same seed → identical render every time\n(offline and live). NB: the seed lives on the pattern, so every\nplacement of the pattern humanizes identically — per-placement variation\nis the deferred per-`PatternPlacement` override (NP0), which would mix a\nplacement nonce into the per-note key the way the engine's probability\nroll mixes `roll_nonce`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "velocity": {
          "$ref": "#/$defs/NormalizedValue",
          "default": 0.15000000596046448,
          "description": "Max velocity deviation as a ± fraction of the note's velocity (0 = off)."
        }
      },
      "type": "object"
    },
    "InstrumentId": {
      "description": "Unique identifier for an instrument.\n\nA single id namespace shared by the sequencer data model, the engine, the\nproject format, the MCP surface, and the GUI. Each instrument gets a unique\nid that persists for its lifetime; ids are never reused within a session.\n\n`InstrumentId(0)` (also [`InstrumentId::FIRST`] and the `Default`) is the\nfirst/default instrument.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "InstrumentState": {
      "description": "Serializable state of a single instrument, used in project files.\n\nUses domain newtypes where they have `#[serde(transparent)]` (Gain,\nBipolarValue, Semitones, InstrumentId). Fields that would change\nserialization format (channel, key_range) or lack serde (oversampling)\nremain as primitives.",
      "properties": {
        "allocation_mode": {
          "$ref": "#/$defs/AllocationMode",
          "default": "Polyphonic",
          "description": "Voice allocation mode (Poly / Mono / Legato / Unison)."
        },
        "category": {
          "default": 0,
          "description": "Category for visualization/routing, stored as the\n`InstrumentCategory::as_u8()` value. `0` = Uncategorized.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "channel": {
          "description": "MIDI channel (1-indexed, 1–16), or 0 for OMNI.",
          "format": "uint8",
          "maximum": 16,
          "minimum": 0,
          "type": "integer"
        },
        "color": {
          "description": "Optional accent color as hex string (e.g. \"#FF8800FF\").",
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "description": "Free-text description of the instrument.",
          "type": "string"
        },
        "id": {
          "$ref": "#/$defs/InstrumentId",
          "description": "Engine instrument ID."
        },
        "key_range": {
          "default": [
            0,
            127
          ],
          "description": "Key range as (low, high) MIDI note numbers.",
          "maxItems": 2,
          "minItems": 2,
          "prefixItems": [
            {
              "format": "uint8",
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            },
            {
              "format": "uint8",
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            }
          ],
          "type": "array"
        },
        "max_voices": {
          "default": 8,
          "description": "Maximum polyphony for this instrument (1–128, default 8).",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "muted": {
          "description": "Whether the instrument is muted.",
          "type": "boolean"
        },
        "name": {
          "description": "Display name.",
          "type": "string"
        },
        "oversampling": {
          "default": 1,
          "description": "Oversampling factor (1, 2, or 4).",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "pan": {
          "description": "Stereo pan (-1.0 = left, 0.0 = center, +1.0 = right).",
          "format": "float",
          "type": "number"
        },
        "patch": {
          "$ref": "#/$defs/Patch",
          "description": "Full module graph for this instrument."
        },
        "sidechain_source_id": {
          "description": "Sidechain source — when set, this instrument's compressors\nreceive audio from the source instrument's output.\nSerialized as `u64` for forward-compat with non-Rust readers.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "solo": {
          "description": "Whether the instrument is soloed.",
          "type": "boolean"
        },
        "stealing_strategy": {
          "$ref": "#/$defs/StealingStrategy",
          "default": "Oldest",
          "description": "Strategy for stealing voices when all are busy."
        },
        "transpose": {
          "default": 0.0,
          "description": "Transpose offset in semitones.",
          "format": "float",
          "type": "number"
        },
        "unison_detune": {
          "default": 10.0,
          "description": "Total unison detune spread (cents), used in `Unison` allocation mode.\nDefaults to 10 cents for projects saved before this field existed.",
          "format": "float",
          "type": "number"
        },
        "unison_spread": {
          "default": 0.0,
          "description": "Unison stereo spread (0..1), used in `Unison` allocation mode. Defaults to\n0 (no spread) for projects saved before this field existed.",
          "format": "float",
          "type": "number"
        },
        "velocity_amp_sensitivity": {
          "default": 1.0,
          "description": "Velocity → amplitude sensitivity (0 = constant volume, 1 = full dynamic).",
          "format": "float",
          "type": "number"
        },
        "velocity_filter_sensitivity": {
          "default": 0.0,
          "description": "Velocity → filter cutoff sensitivity (0 = none, 1 = full).",
          "format": "float",
          "type": "number"
        },
        "volume": {
          "description": "Volume (0.0 = silent, 1.0 = unity).",
          "format": "float",
          "type": "number"
        }
      },
      "required": [
        "id",
        "name",
        "channel",
        "volume",
        "pan",
        "muted",
        "solo",
        "patch"
      ],
      "type": "object"
    },
    "LfoShape": {
      "description": "Shape of a [`NoteLfo`]'s unipolar (`0.0..1.0`) waveform over one cycle.",
      "oneOf": [
        {
          "const": "Sine",
          "description": "Raised sine: `0.5 + 0.5·sin`.",
          "type": "string"
        },
        {
          "const": "Triangle",
          "description": "Rising-then-falling triangle.",
          "type": "string"
        },
        {
          "const": "Saw",
          "description": "Rising ramp (`= phase`).",
          "type": "string"
        },
        {
          "const": "Square",
          "description": "High for the first half of the cycle, low for the second.",
          "type": "string"
        }
      ]
    },
    "LoopRegion": {
      "description": "A persisted transport loop region: playback wraps from `end` back to `start`\nwhile `enabled`. This is the serialized carrier for the loop the engine's\nsequencer owns at runtime — captured at save time and restored (via a\n`SetLoop` command) at load time, so a saved project keeps its loop instead of\nsilently dropping it.",
      "properties": {
        "enabled": {
          "description": "Whether the loop is active. A region can be stored while disabled so the\nbounds survive a toggle.",
          "type": "boolean"
        },
        "end": {
          "$ref": "#/$defs/Tick",
          "description": "Loop end position (exclusive; playback wraps here back to `start`)."
        },
        "start": {
          "$ref": "#/$defs/Tick",
          "description": "Loop start position."
        }
      },
      "required": [
        "start",
        "end",
        "enabled"
      ],
      "type": "object"
    },
    "MacroNode": {
      "description": "A grid-native named knob — the song-level macro bank.",
      "properties": {
        "name": {
          "description": "Display name of the macro.",
          "type": "string"
        },
        "value": {
          "$ref": "#/$defs/NormalizedValue",
          "default": 0.0,
          "description": "Current value, `0.0..=1.0`."
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "MidiCcNode": {
      "description": "A grid-native MIDI CC input source node.",
      "properties": {
        "cc": {
          "$ref": "#/$defs/MidiCcNumber",
          "default": 0,
          "description": "Controller number, `0..=127`."
        },
        "channel": {
          "description": "MIDI channel to listen on, or `None` for omni.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "MidiCcNumber": {
      "description": "Standard MIDI continuous-controller number (`0..=127`).\n\nUnlike [`CcNumber`], this type excludes the pseudo-CC values used by the\ntelemetry subsystem.",
      "format": "uint8",
      "maximum": 127,
      "minimum": 0,
      "type": "integer"
    },
    "Milliseconds": {
      "description": "Duration in milliseconds.\n\nConvenience type for UI display and input.",
      "format": "float",
      "type": "number"
    },
    "ModConnection": {
      "description": "A control-signal cable between two nodes' named ports.",
      "properties": {
        "from": {
          "$ref": "#/$defs/ModNodeId",
          "description": "Source node."
        },
        "from_port": {
          "description": "Output port name on the source node (e.g. `\"out\"`).",
          "type": "string"
        },
        "to": {
          "$ref": "#/$defs/ModNodeId",
          "description": "Destination node."
        },
        "to_port": {
          "description": "Input port name on the destination node (e.g. `\"in\"`, `\"rate_cv\"`).",
          "type": "string"
        }
      },
      "required": [
        "from",
        "from_port",
        "to",
        "to_port"
      ],
      "type": "object"
    },
    "ModGraph": {
      "description": "Static mod-graph configuration — a pooled project asset.",
      "properties": {
        "assigned_tracks": {
          "description": "Tracks this graph is assigned to (Track scope only). One running instance\nper assigned track; \"this track\" targets resolve to each host.",
          "items": {
            "$ref": "#/$defs/TrackId"
          },
          "type": "array"
        },
        "color": {
          "anyOf": [
            {
              "$ref": "#/$defs/TrackColor"
            },
            {
              "type": "null"
            }
          ],
          "description": "Sequencer-side color (RGB), never egui's `Color32` — this crate has no\negui dependency."
        },
        "connections": {
          "default": [],
          "description": "Cables, kept as a sorted `Vec` for deterministic serialization.",
          "items": {
            "$ref": "#/$defs/ModConnection"
          },
          "type": "array"
        },
        "description": {
          "default": "",
          "description": "Free-text description.",
          "type": "string"
        },
        "id": {
          "$ref": "#/$defs/ModGraphId",
          "description": "Pool-unique id."
        },
        "name": {
          "description": "User-facing name, shown in the pool view.",
          "type": "string"
        },
        "node_descriptions": {
          "additionalProperties": false,
          "description": "Pedagogical/user intent per node, kept separate from DSP configs.",
          "patternProperties": {
            "^\\d+$": {
              "type": "string"
            }
          },
          "type": "object"
        },
        "node_positions": {
          "additionalProperties": false,
          "description": "Editor canvas positions per node (layout metadata; nodes without one are\nauto-laid-out). Persisted so an arranged canvas survives reload.",
          "patternProperties": {
            "^\\d+$": {
              "$ref": "#/$defs/NodePosition"
            }
          },
          "type": "object"
        },
        "nodes": {
          "additionalProperties": false,
          "default": {},
          "description": "Nodes keyed by graph-local id.",
          "patternProperties": {
            "^\\d+$": {
              "$ref": "#/$defs/ModNodeConfig"
            }
          },
          "type": "object"
        },
        "scope": {
          "$ref": "#/$defs/ModGraphScope",
          "default": "Global",
          "description": "Global (one always-on instance) or Track (one instance per assignment)."
        }
      },
      "required": [
        "id",
        "name"
      ],
      "type": "object"
    },
    "ModGraphId": {
      "description": "Unique identifier for a Mod Grid graph in the project pool.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "ModGraphScope": {
      "description": "Where a mod graph runs and how \"this track\" targets resolve.",
      "oneOf": [
        {
          "const": "Global",
          "description": "Exactly one running instance, always on. Relative `Track { track: None }`\ntargets cannot resolve (no host) and are dropped.",
          "type": "string"
        },
        {
          "const": "Track",
          "description": "Pooled and assigned to zero or more tracks; one running instance per\nassignment, and relative \"this track\" targets resolve to the host track.",
          "type": "string"
        }
      ]
    },
    "ModNodeConfig": {
      "description": "A single node's configuration.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "A hosted control-rate audio module (LFO, MSEG, envelope follower, …).",
          "properties": {
            "Module": {
              "$ref": "#/$defs/ModuleNode"
            }
          },
          "required": [
            "Module"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A song-level macro knob.",
          "properties": {
            "Macro": {
              "$ref": "#/$defs/MacroNode"
            }
          },
          "required": [
            "Macro"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A transport-derived source (beat/bar phase, tempo, position).",
          "properties": {
            "Transport": {
              "$ref": "#/$defs/TransportNode"
            }
          },
          "required": [
            "Transport"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A MIDI CC input source.",
          "properties": {
            "MidiCc": {
              "$ref": "#/$defs/MidiCcNode"
            }
          },
          "required": [
            "MidiCc"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A pre-fader level tap of a track or the master bus.",
          "properties": {
            "AudioTap": {
              "$ref": "#/$defs/AudioTapNode"
            }
          },
          "required": [
            "AudioTap"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A routing sink into an [`AutomationTarget`].",
          "properties": {
            "Target": {
              "$ref": "#/$defs/ModTarget"
            }
          },
          "required": [
            "Target"
          ],
          "type": "object"
        }
      ]
    },
    "ModNodeId": {
      "description": "Unique identifier for a node within a single [`ModGraphId`] graph.\n\nOnly unique within its owning graph — different graphs reuse the same small\nid space.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "ModTarget": {
      "description": "A routing sink: writes its input control signal into an automation target.",
      "properties": {
        "amount": {
          "$ref": "#/$defs/ModulationAmount",
          "default": 0.0,
          "description": "Depth in the target's own units (semitones for pitch, `0..1` for a\nnormalized param, etc.). The input signal is multiplied by this."
        },
        "combine": {
          "$ref": "#/$defs/CombineMode",
          "default": "Add",
          "description": "How the contribution composes with the target's current value."
        },
        "target": {
          "$ref": "#/$defs/AutomationTarget",
          "description": "The address written to. Relative `Track { track: None }` resolves to the\ninstance's host track (Track scope) or is dropped (Global scope)."
        }
      },
      "required": [
        "target"
      ],
      "type": "object"
    },
    "ModulationAmount": {
      "description": "Target-relative modulation depth.\n\nThe numeric unit is defined by the destination (normalized units for gain,\nbipolar units for pan, semitones for pitch). Keeping the value wrapped\nprevents it from being confused with an already-resolved control value.",
      "format": "float",
      "type": "number"
    },
    "ModuleGroupState": {
      "description": "Group of modules in the patch editor (UI-level metadata).",
      "properties": {
        "collapsed": {
          "description": "Whether the group is collapsed in the UI.",
          "type": "boolean"
        },
        "color": {
          "type": [
            "string",
            "null"
          ]
        },
        "exposed_inputs": {
          "items": {
            "$ref": "#/$defs/ExposedPortState"
          },
          "type": "array"
        },
        "exposed_outputs": {
          "items": {
            "$ref": "#/$defs/ExposedPortState"
          },
          "type": "array"
        },
        "id": {
          "$ref": "#/$defs/GroupId"
        },
        "members": {
          "description": "Module IDs that belong to this group.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "name": {
          "type": "string"
        },
        "position": {
          "$ref": "#/$defs/Position",
          "description": "Position of the group box when collapsed."
        }
      },
      "required": [
        "id",
        "name",
        "members",
        "collapsed",
        "position"
      ],
      "type": "object"
    },
    "ModuleNode": {
      "description": "A hosted control-rate audio module, run mono by the grid harness.",
      "properties": {
        "module_type": {
          "$ref": "#/$defs/ModuleType",
          "description": "Which module type to host (e.g. `Lfo`, `Mseg`, `EnvelopeFollower`)."
        },
        "params": {
          "additionalProperties": {
            "format": "float",
            "type": "number"
          },
          "default": {},
          "description": "Parameter values, keyed by descriptor `type_id`. Stored as `f32` (the\nsame boundary the MCP `set_parameter` uses); choice params encode as\ntheir choice index.",
          "type": "object"
        },
        "seed": {
          "description": "Explicit persisted seed for random-family modules, so an offline render\nreproduces the live result bit-for-bit. `None` for deterministic modules.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "module_type"
      ],
      "type": "object"
    },
    "ModuleState": {
      "description": "State of a single module. The `type` discriminator selects which parameter set is valid.",
      "oneOf": [
        {
          "description": "Oscillator (oscillator) — Band-limited oscillator with multiple waveforms and unison",
          "examples": [
            {
              "id": "osc-1",
              "parameters": {
                "detune": 0.0,
                "frequency": 440.0,
                "level": 1.0,
                "waveform": "sawtooth"
              },
              "position": {
                "x": 32.0,
                "y": 32.0
              },
              "type": "oscillator"
            }
          ],
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `osc-1`.",
              "pattern": "^osc-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "anti_alias": {
                  "description": "Anti-aliasing algorithm",
                  "enum": [
                    "polyblep",
                    "minblep",
                    "raw"
                  ],
                  "title": "Anti-Alias",
                  "type": "string"
                },
                "detune": {
                  "default": 0.0,
                  "description": "Fine tune in cents — unit: ct",
                  "maximum": 100.0,
                  "minimum": -100.0,
                  "title": "Detune",
                  "type": "number"
                },
                "fm_amt": {
                  "default": 1.0,
                  "description": "FM input attenuverter (-1 to +1)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "FM Amt",
                  "type": "number"
                },
                "fm_mode": {
                  "description": "FM input mode: Exponential (1V/oct) or Linear (Hz)",
                  "enum": [
                    "exponential",
                    "linear"
                  ],
                  "title": "FM Mode",
                  "type": "string"
                },
                "frequency": {
                  "default": 440.0,
                  "description": "Base frequency — unit: Hz",
                  "maximum": 20000.0,
                  "minimum": 1.0,
                  "title": "Frequency",
                  "type": "number"
                },
                "glide_time": {
                  "default": 0.0,
                  "description": "Per-oscillator portamento time (0 = follow the voice glide) — unit: s",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Glide",
                  "type": "number"
                },
                "level": {
                  "default": 1.0,
                  "description": "Output level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "octave": {
                  "default": 0.0,
                  "description": "Coarse tune in semitones — unit: st",
                  "maximum": 24.0,
                  "minimum": -24.0,
                  "title": "Octave",
                  "type": "number"
                },
                "phase": {
                  "default": 0.0,
                  "description": "Start phase offset (0–1 turns)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Phase",
                  "type": "number"
                },
                "pulse_width": {
                  "default": 0.5,
                  "description": "Pulse width for pulse waveform — unit: %",
                  "maximum": 0.9900000095367432,
                  "minimum": 0.009999999776482582,
                  "title": "Pulse Width",
                  "type": "number"
                },
                "uni_detune": {
                  "default": 10.0,
                  "description": "Unison detune spread in cents — unit: ct",
                  "maximum": 100.0,
                  "minimum": 0.0,
                  "title": "Uni Detune",
                  "type": "number"
                },
                "uni_phase": {
                  "default": 1.0,
                  "description": "Phase randomization on note-on",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Uni Phase",
                  "type": "number"
                },
                "uni_spread": {
                  "default": 0.5,
                  "description": "Unison stereo spread (0 = mono, 1 = full)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Uni Spread",
                  "type": "number"
                },
                "unison": {
                  "default": 1.0,
                  "description": "Number of unison voices (1 = off)",
                  "maximum": 7.0,
                  "minimum": 1.0,
                  "title": "Unison",
                  "type": "number"
                },
                "waveform": {
                  "description": "Oscillator waveform",
                  "enum": [
                    "sine",
                    "triangle",
                    "sawtooth",
                    "square",
                    "pulse",
                    "dsf_saw"
                  ],
                  "title": "Waveform",
                  "type": "string"
                },
                "x_mod": {
                  "default": 0.0,
                  "description": "Cross-modulation amount (0 = off, 1 = full)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "X-Mod",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "oscillator",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Oscillator",
          "type": "object"
        },
        {
          "description": "Math Oscillator (math_oscillator) — Advanced oscillator with 19 mathematical synthesis algorithms",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `mth-1`.",
              "pattern": "^mth-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "algorithm": {
                  "description": "Synthesis algorithm",
                  "enum": [
                    "sine_fm",
                    "tan_chaos",
                    "super_saw",
                    "bitwise",
                    "wave_folder",
                    "formant",
                    "phase_dist",
                    "metallic",
                    "fractal",
                    "chebyshev",
                    "bytebeat",
                    "lorenz",
                    "karplus_strong",
                    "walsh",
                    "logistic",
                    "pulsar",
                    "shepard",
                    "feedback_fm",
                    "vosim"
                  ],
                  "title": "Algorithm",
                  "type": "string"
                },
                "frequency": {
                  "default": 440.0,
                  "description": "Base frequency — unit: Hz",
                  "maximum": 20000.0,
                  "minimum": 20.0,
                  "title": "Frequency",
                  "type": "number"
                },
                "glide_time": {
                  "default": 0.0,
                  "description": "Per-oscillator portamento time (0 = follow the voice glide) — unit: s",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Glide",
                  "type": "number"
                },
                "level": {
                  "default": 1.0,
                  "description": "Output level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "param_a": {
                  "default": 0.5,
                  "description": "Algorithm parameter A",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Param A",
                  "type": "number"
                },
                "param_b": {
                  "default": 0.5,
                  "description": "Algorithm parameter B",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Param B",
                  "type": "number"
                },
                "param_c": {
                  "default": 0.5,
                  "description": "Algorithm parameter C",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Param C",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "math_oscillator",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Math Oscillator",
          "type": "object"
        },
        {
          "description": "Sub Osc (sub_oscillator) — Sub-oscillator for bass reinforcement at -1 or -2 octaves",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `sub-1`.",
              "pattern": "^sub-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "glide_time": {
                  "default": 0.0,
                  "description": "Per-oscillator portamento time (0 = follow the voice glide) — unit: s",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Glide",
                  "type": "number"
                },
                "level": {
                  "default": 0.5,
                  "description": "Sub-oscillator output level — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "octave": {
                  "description": "Octave transposition",
                  "enum": [
                    "minus1",
                    "minus2"
                  ],
                  "title": "Octave",
                  "type": "string"
                },
                "waveform": {
                  "description": "Sub-oscillator waveform",
                  "enum": [
                    "sine",
                    "triangle",
                    "sawtooth",
                    "square",
                    "pulse25",
                    "dsf_saw"
                  ],
                  "title": "Waveform",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "sub_oscillator",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Sub Osc",
          "type": "object"
        },
        {
          "description": "Noise (noise) — Spectrally colored noise generator for textures and percussion",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `nse-1`.",
              "pattern": "^nse-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "level": {
                  "default": 0.800000011920929,
                  "description": "Output level — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "type": {
                  "description": "Noise color/spectrum",
                  "enum": [
                    "white",
                    "pink",
                    "brown",
                    "blue",
                    "violet",
                    "lfsr",
                    "chip"
                  ],
                  "title": "Type",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "noise",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Noise",
          "type": "object"
        },
        {
          "description": "Filter (filter) — State Variable Filter with multiple modes",
          "examples": [
            {
              "id": "flt-1",
              "parameters": {
                "cutoff": 0.5,
                "resonance": 0.3,
                "type": "lowpass"
              },
              "position": {
                "x": 224.0,
                "y": 32.0
              },
              "type": "filter"
            }
          ],
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `flt-1`.",
              "pattern": "^flt-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "cutoff": {
                  "default": 1000.0,
                  "description": "Cutoff frequency — unit: Hz",
                  "maximum": 20000.0,
                  "minimum": 20.0,
                  "title": "Cutoff",
                  "type": "number"
                },
                "cv_amt": {
                  "default": 1.0,
                  "description": "Cutoff CV input attenuverter (-1 to +1)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "CV Amt",
                  "type": "number"
                },
                "drive": {
                  "default": 1.0,
                  "description": "Input gain with soft saturation (above 1.0)",
                  "maximum": 4.0,
                  "minimum": 0.5,
                  "title": "Drive",
                  "type": "number"
                },
                "env_amt": {
                  "default": 0.25,
                  "description": "Envelope/CV amount in semitones (-1 = -48 st, +1 = +48 st). Default 0.25 ≈ 1 octave at full envelope.",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Env Amt",
                  "type": "number"
                },
                "key_track": {
                  "default": 0.0,
                  "description": "Keyboard tracking amount — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Key Track",
                  "type": "number"
                },
                "model": {
                  "description": "Filter character model",
                  "enum": [
                    "standard",
                    "fluid",
                    "screamer",
                    "acid",
                    "karlsen"
                  ],
                  "title": "Model",
                  "type": "string"
                },
                "morph": {
                  "default": 0.0,
                  "description": "Fluid: LP→BP→HP→Notch crossfade",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Morph",
                  "type": "number"
                },
                "resonance": {
                  "default": 0.0,
                  "description": "Filter resonance (Q)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Resonance",
                  "type": "number"
                },
                "type": {
                  "description": "Filter type",
                  "enum": [
                    "lowpass",
                    "highpass",
                    "bandpass",
                    "notch",
                    "peak",
                    "low_shelf",
                    "high_shelf"
                  ],
                  "title": "Type",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "filter",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Filter",
          "type": "object"
        },
        {
          "description": "ADSR (envelope) — ADSR envelope generator",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `env-1`.",
              "pattern": "^env-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "atk_curve": {
                  "default": 0.0,
                  "description": "Attack curve shape (-1 = logarithmic, 0 = linear, +1 = exponential)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Atk Curve",
                  "type": "number"
                },
                "attack": {
                  "default": 0.009999999776482582,
                  "description": "Attack time (silence to peak) — unit: s",
                  "maximum": 10.0,
                  "minimum": 0.0,
                  "title": "Attack",
                  "type": "number"
                },
                "dec_curve": {
                  "default": 0.0,
                  "description": "Decay curve shape (-1 = logarithmic, 0 = linear, +1 = exponential)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Dec Curve",
                  "type": "number"
                },
                "decay": {
                  "default": 0.10000000149011612,
                  "description": "Decay time (peak to sustain) — unit: s",
                  "maximum": 10.0,
                  "minimum": 0.0,
                  "title": "Decay",
                  "type": "number"
                },
                "rel_curve": {
                  "default": 0.0,
                  "description": "Release curve shape (-1 = logarithmic, 0 = linear, +1 = exponential)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Rel Curve",
                  "type": "number"
                },
                "release": {
                  "default": 0.30000001192092896,
                  "description": "Release time (gate-off to silence) — unit: s",
                  "maximum": 10.0,
                  "minimum": 0.0,
                  "title": "Release",
                  "type": "number"
                },
                "sustain": {
                  "default": 0.699999988079071,
                  "description": "Sustain level while gate is held — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Sustain",
                  "type": "number"
                },
                "time_scale": {
                  "default": 1.0,
                  "description": "Global multiplier for attack, decay, and release times (0.01-10.0)",
                  "maximum": 10.0,
                  "minimum": 0.009999999776482582,
                  "title": "Time Scale",
                  "type": "number"
                },
                "vel_sens": {
                  "default": 1.0,
                  "description": "How much note velocity scales the envelope (0 = ignore, 1 = full)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Vel Sens",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "envelope",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "ADSR",
          "type": "object"
        },
        {
          "description": "LFO (lfo) — Low Frequency Oscillator",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `lfo-1`.",
              "pattern": "^lfo-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "depth": {
                  "default": 1.0,
                  "description": "Modulation depth (0 = off, 1 = full)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Depth",
                  "type": "number"
                },
                "phase": {
                  "default": 0.0,
                  "description": "Starting phase of the LFO cycle",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Phase",
                  "type": "number"
                },
                "rate": {
                  "default": 1.0,
                  "description": "LFO rate — unit: Hz",
                  "maximum": 50.0,
                  "minimum": 0.009999999776482582,
                  "title": "Rate",
                  "type": "number"
                },
                "retrigger": {
                  "default": 0.0,
                  "description": "Restart the cycle on the Retrig gate edge",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Retrigger",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "sync_division": {
                  "default": 1.0,
                  "description": "Beats per LFO cycle when tempo-synced (1 = quarter note) — unit: beats",
                  "maximum": 4.0,
                  "minimum": 0.125,
                  "title": "Division",
                  "type": "number"
                },
                "tempo_sync": {
                  "default": 0.0,
                  "description": "Sync rate to host tempo (uses Division instead of Rate)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Tempo Sync",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "waveform": {
                  "description": "LFO waveform",
                  "enum": [
                    "sine",
                    "triangle",
                    "sawtooth",
                    "square",
                    "sample_and_hold",
                    "smooth_random"
                  ],
                  "title": "Waveform",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "lfo",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "LFO",
          "type": "object"
        },
        {
          "description": "VCA (amplifier) — Voltage Controlled Amplifier",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `amp-1`.",
              "pattern": "^amp-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "cv_bipolar": {
                  "default": 0.0,
                  "description": "Allow negative CV for ring modulation",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "CV Bipolar",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "level": {
                  "default": 1.0,
                  "description": "Output gain (0 = silent, 1 = unity, 2 = +6 dB)",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "pan": {
                  "default": 0.0,
                  "description": "Stereo pan (-1 = left, 0 = center, +1 = right)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Pan",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "amplifier",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "VCA",
          "type": "object"
        },
        {
          "description": "Mixer (mixer) — 8-channel mixer",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `mix-1`.",
              "pattern": "^mix-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "input_1": {
                  "default": 1.0,
                  "description": "Gain for input 1",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Input 1",
                  "type": "number"
                },
                "input_2": {
                  "default": 1.0,
                  "description": "Gain for input 2",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Input 2",
                  "type": "number"
                },
                "input_3": {
                  "default": 1.0,
                  "description": "Gain for input 3",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Input 3",
                  "type": "number"
                },
                "input_4": {
                  "default": 1.0,
                  "description": "Gain for input 4",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Input 4",
                  "type": "number"
                },
                "input_5": {
                  "default": 1.0,
                  "description": "Gain for input 5",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Input 5",
                  "type": "number"
                },
                "input_6": {
                  "default": 1.0,
                  "description": "Gain for input 6",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Input 6",
                  "type": "number"
                },
                "input_7": {
                  "default": 1.0,
                  "description": "Gain for input 7",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Input 7",
                  "type": "number"
                },
                "input_8": {
                  "default": 1.0,
                  "description": "Gain for input 8",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Input 8",
                  "type": "number"
                },
                "limit": {
                  "default": 0.0,
                  "description": "Soft tanh output limiter",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Limit",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "master": {
                  "default": 1.0,
                  "description": "Master output gain",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Master",
                  "type": "number"
                },
                "mute": {
                  "default": 0.0,
                  "description": "Silence the mixer output",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mute",
                  "type": [
                    "number",
                    "boolean"
                  ]
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "mixer",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Mixer",
          "type": "object"
        },
        {
          "description": "Stereo Output (stereo_output) — Master stereo output with volume, pan, and limiting",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `out-1`.",
              "pattern": "^out-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "dither": {
                  "default": 0.0,
                  "description": "Enable 24-bit noise-shaped dithering",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Dither",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "limit": {
                  "default": 1.0,
                  "description": "Enable soft limiter to prevent clipping",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Limit",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "master": {
                  "default": 0.800000011920929,
                  "description": "Master output volume",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Master",
                  "type": "number"
                },
                "mute": {
                  "default": 0.0,
                  "description": "Mute output",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mute",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "pan": {
                  "default": 0.0,
                  "description": "Master stereo pan",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Pan",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "stereo_output",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Stereo Output",
          "type": "object"
        },
        {
          "description": "Delay (delay) — Stereo delay with ping-pong mode",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `dly-1`.",
              "pattern": "^dly-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "feedback": {
                  "default": 0.4000000059604645,
                  "description": "Feedback amount",
                  "maximum": 0.949999988079071,
                  "minimum": 0.0,
                  "title": "Feedback",
                  "type": "number"
                },
                "mix": {
                  "default": 0.5,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "mode": {
                  "description": "Delay mode",
                  "enum": [
                    "mono",
                    "stereo",
                    "ping_pong"
                  ],
                  "title": "Mode",
                  "type": "string"
                },
                "sync_division": {
                  "default": 1.0,
                  "description": "Beats per echo when tempo-synced (1 = quarter note) — unit: beats",
                  "maximum": 4.0,
                  "minimum": 0.125,
                  "title": "Division",
                  "type": "number"
                },
                "tempo_sync": {
                  "default": 0.0,
                  "description": "Sync delay time to host tempo (uses Division instead of Time)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Tempo Sync",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "time": {
                  "default": 0.375,
                  "description": "Delay time (link: sets both left and right) — unit: s",
                  "maximum": 2.0,
                  "minimum": 0.0010000000474974513,
                  "title": "Time",
                  "type": "number"
                },
                "time_left": {
                  "default": 0.375,
                  "description": "Left-channel delay time — unit: s",
                  "maximum": 2.0,
                  "minimum": 0.0010000000474974513,
                  "title": "Time L",
                  "type": "number"
                },
                "time_right": {
                  "default": 0.5,
                  "description": "Right-channel delay time — unit: s",
                  "maximum": 2.0,
                  "minimum": 0.0010000000474974513,
                  "title": "Time R",
                  "type": "number"
                },
                "tone": {
                  "default": 0.4000000059604645,
                  "description": "Feedback high-cut filter",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Tone",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "delay",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Delay",
          "type": "object"
        },
        {
          "description": "Reverb (reverb) — FDN reverb with 8-channel feedback delay network",
          "examples": [
            {
              "id": "rev-1",
              "parameters": {
                "damping": 0.6,
                "decay": 2.0,
                "mix": 0.3
              },
              "position": {
                "x": 1760.0,
                "y": 32.0
              },
              "type": "reverb"
            }
          ],
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `rev-1`.",
              "pattern": "^rev-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "damping": {
                  "default": 0.5,
                  "description": "High frequency absorption in feedback path",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Damping",
                  "type": "number"
                },
                "decay": {
                  "default": 0.5,
                  "description": "Reverb tail length (feedback gain)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Decay",
                  "type": "number"
                },
                "diffusion": {
                  "default": 0.5,
                  "description": "Delay modulation for density (0=sparse, 1=dense)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Diffusion",
                  "type": "number"
                },
                "low_cut": {
                  "default": 80.0,
                  "description": "Highpass filter frequency for reverb tail — unit: Hz",
                  "maximum": 500.0,
                  "minimum": 20.0,
                  "title": "Low Cut",
                  "type": "number"
                },
                "mix": {
                  "default": 0.30000001192092896,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "pre_delay": {
                  "default": 0.0,
                  "description": "Initial delay before reverb onset — unit: s",
                  "maximum": 0.5,
                  "minimum": 0.0,
                  "title": "Pre-Delay",
                  "type": "number"
                },
                "room_size": {
                  "default": 0.5,
                  "description": "Size of the virtual room (scales delay times)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Room Size",
                  "type": "number"
                },
                "width": {
                  "default": 1.0,
                  "description": "Stereo width (0=mono, 1=full stereo)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Width",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "reverb",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Reverb",
          "type": "object"
        },
        {
          "description": "Distortion (distortion) — Multi-mode distortion effect",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `dst-1`.",
              "pattern": "^dst-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "bit_depth": {
                  "default": 8.0,
                  "description": "Bit depth for bitcrush mode (1-16 bits)",
                  "maximum": 16.0,
                  "minimum": 1.0,
                  "title": "Bit Depth",
                  "type": "number"
                },
                "drive": {
                  "default": 0.5,
                  "description": "Distortion amount",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Drive",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "tone": {
                  "default": 0.800000011920929,
                  "description": "Post-distortion filter",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Tone",
                  "type": "number"
                },
                "type": {
                  "description": "Distortion algorithm",
                  "enum": [
                    "soft_clip",
                    "hard_clip",
                    "tube",
                    "foldback",
                    "bitcrush",
                    "variable_clip"
                  ],
                  "title": "Type",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "distortion",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Distortion",
          "type": "object"
        },
        {
          "description": "Chorus (chorus) — Multi-voice chorus effect",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `chr-1`.",
              "pattern": "^chr-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "depth": {
                  "default": 0.5,
                  "description": "Modulation depth",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Depth",
                  "type": "number"
                },
                "mix": {
                  "default": 0.5,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "rate": {
                  "default": 0.5,
                  "description": "LFO rate — unit: Hz",
                  "maximum": 5.0,
                  "minimum": 0.10000000149011612,
                  "title": "Rate",
                  "type": "number"
                },
                "voices": {
                  "default": 2.0,
                  "description": "Number of chorus voices",
                  "maximum": 4.0,
                  "minimum": 1.0,
                  "title": "Voices",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "chorus",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Chorus",
          "type": "object"
        },
        {
          "description": "Phaser (phaser) — Classic phaser effect with LFO modulation",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `phs-1`.",
              "pattern": "^phs-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "center_freq": {
                  "default": 1000.0,
                  "description": "Center frequency — unit: Hz",
                  "maximum": 4000.0,
                  "minimum": 100.0,
                  "title": "Center Freq",
                  "type": "number"
                },
                "depth": {
                  "default": 0.699999988079071,
                  "description": "Modulation depth",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Depth",
                  "type": "number"
                },
                "feedback": {
                  "default": 0.699999988079071,
                  "description": "Feedback amount",
                  "maximum": 0.949999988079071,
                  "minimum": -0.949999988079071,
                  "title": "Feedback",
                  "type": "number"
                },
                "mix": {
                  "default": 0.5,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "rate": {
                  "default": 0.5,
                  "description": "LFO rate — unit: Hz",
                  "maximum": 5.0,
                  "minimum": 0.05000000074505806,
                  "title": "Rate",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "phaser",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Phaser",
          "type": "object"
        },
        {
          "description": "Flanger (flanger) — Classic flanger effect with feedback",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `fln-1`.",
              "pattern": "^fln-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "delay": {
                  "default": 2.0,
                  "description": "Base delay time — unit: ms",
                  "maximum": 10.0,
                  "minimum": 0.10000000149011612,
                  "title": "Delay",
                  "type": "number"
                },
                "depth": {
                  "default": 0.699999988079071,
                  "description": "Modulation depth",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Depth",
                  "type": "number"
                },
                "feedback": {
                  "default": 0.699999988079071,
                  "description": "Feedback amount",
                  "maximum": 0.949999988079071,
                  "minimum": -0.949999988079071,
                  "title": "Feedback",
                  "type": "number"
                },
                "mix": {
                  "default": 0.5,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "rate": {
                  "default": 0.30000001192092896,
                  "description": "LFO rate — unit: Hz",
                  "maximum": 5.0,
                  "minimum": 0.05000000074505806,
                  "title": "Rate",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "flanger",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Flanger",
          "type": "object"
        },
        {
          "description": "Compressor (compressor) — Dynamics compressor with adjustable attack and release",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `cmp-1`.",
              "pattern": "^cmp-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "attack": {
                  "default": 10.0,
                  "description": "Attack time — unit: ms",
                  "maximum": 100.0,
                  "minimum": 0.10000000149011612,
                  "title": "Attack",
                  "type": "number"
                },
                "makeup": {
                  "default": 0.0,
                  "description": "Makeup gain — unit: dB",
                  "maximum": 24.0,
                  "minimum": 0.0,
                  "title": "Makeup",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix (parallel compression)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "ratio": {
                  "default": 4.0,
                  "description": "Compression ratio — unit: :1",
                  "maximum": 20.0,
                  "minimum": 1.0,
                  "title": "Ratio",
                  "type": "number"
                },
                "release": {
                  "default": 100.0,
                  "description": "Release time — unit: ms",
                  "maximum": 1000.0,
                  "minimum": 10.0,
                  "title": "Release",
                  "type": "number"
                },
                "sc_filter": {
                  "default": 80.0,
                  "description": "Sidechain high-pass filter frequency — unit: Hz",
                  "maximum": 500.0,
                  "minimum": 20.0,
                  "title": "SC Filter",
                  "type": "number"
                },
                "sidechain": {
                  "default": 0.0,
                  "description": "Enable external sidechain input for detection",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Sidechain",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "threshold": {
                  "default": -20.0,
                  "description": "Compression threshold — unit: dB",
                  "maximum": 0.0,
                  "minimum": -60.0,
                  "title": "Threshold",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "compressor",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Compressor",
          "type": "object"
        },
        {
          "description": "EQ (eq) — 3-band parametric equalizer",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `equ-1`.",
              "pattern": "^equ-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "high_freq": {
                  "default": 4000.0,
                  "description": "High shelf frequency — unit: Hz",
                  "maximum": 16000.0,
                  "minimum": 1000.0,
                  "title": "High Freq",
                  "type": "number"
                },
                "high_gain": {
                  "default": 0.0,
                  "description": "High shelf gain — unit: dB",
                  "maximum": 12.0,
                  "minimum": -12.0,
                  "title": "High Gain",
                  "type": "number"
                },
                "low_freq": {
                  "default": 200.0,
                  "description": "Low shelf frequency — unit: Hz",
                  "maximum": 500.0,
                  "minimum": 20.0,
                  "title": "Low Freq",
                  "type": "number"
                },
                "low_gain": {
                  "default": 0.0,
                  "description": "Low shelf gain — unit: dB",
                  "maximum": 12.0,
                  "minimum": -12.0,
                  "title": "Low Gain",
                  "type": "number"
                },
                "mid_freq": {
                  "default": 1000.0,
                  "description": "Mid band frequency — unit: Hz",
                  "maximum": 5000.0,
                  "minimum": 200.0,
                  "title": "Mid Freq",
                  "type": "number"
                },
                "mid_gain": {
                  "default": 0.0,
                  "description": "Mid band gain — unit: dB",
                  "maximum": 12.0,
                  "minimum": -12.0,
                  "title": "Mid Gain",
                  "type": "number"
                },
                "mid_q": {
                  "default": 1.0,
                  "description": "Mid band Q factor",
                  "maximum": 10.0,
                  "minimum": 0.10000000149011612,
                  "title": "Mid Q",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "eq",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "EQ",
          "type": "object"
        },
        {
          "description": "Waveshaper (waveshaper) — Creative waveshaping effect with selectable curves",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `wsh-1`.",
              "pattern": "^wsh-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "bias": {
                  "default": 0.0,
                  "description": "DC offset before shaping",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Bias",
                  "type": "number"
                },
                "curve": {
                  "description": "Waveshaping algorithm",
                  "enum": [
                    "soft_clip",
                    "asymmetric",
                    "fold",
                    "chebyshev",
                    "sine_fold",
                    "quantize"
                  ],
                  "title": "Curve",
                  "type": "string"
                },
                "drive": {
                  "default": 0.30000001192092896,
                  "description": "Input gain before shaping (exponential 1x-20x)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Drive",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "symmetry": {
                  "default": 0.0,
                  "description": "Asymmetric distortion control",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Symmetry",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "waveshaper",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Waveshaper",
          "type": "object"
        },
        {
          "description": "Scope (oscilloscope) — Oscilloscope waveform display",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `scp-1`.",
              "pattern": "^scp-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "freeze": {
                  "default": 0.0,
                  "description": "Freeze the display (stop capturing)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Freeze",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "gain": {
                  "default": 1.0,
                  "description": "Vertical gain",
                  "maximum": 10.0,
                  "minimum": 0.10000000149011612,
                  "title": "Gain",
                  "type": "number"
                },
                "time": {
                  "default": 0.009999999776482582,
                  "description": "Time scale (zoom) — unit: s",
                  "maximum": 0.10000000149011612,
                  "minimum": 0.0010000000474974513,
                  "title": "Time",
                  "type": "number"
                },
                "trig": {
                  "default": 0.5,
                  "description": "Trigger level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Trig",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "oscilloscope",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Scope",
          "type": "object"
        },
        {
          "description": "Meter (level_meter) — Audio level meter with peak hold",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `mtr-1`.",
              "pattern": "^mtr-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "hold": {
                  "default": 1.0,
                  "description": "Peak hold time in seconds — unit: s",
                  "maximum": 5.0,
                  "minimum": 0.10000000149011612,
                  "title": "Hold",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "level_meter",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Meter",
          "type": "object"
        },
        {
          "description": "Spectrum (spectrum_analyzer) — FFT spectrum analyzer display",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `spa-1`.",
              "pattern": "^spa-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "gain": {
                  "default": 1.0,
                  "description": "Vertical gain scaling",
                  "maximum": 10.0,
                  "minimum": 0.10000000149011612,
                  "title": "Gain",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "spectrum_analyzer",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Spectrum",
          "type": "object"
        },
        {
          "description": "Mod Matrix (mod_matrix) — Modulation matrix — a list of source → destination routings",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `mmx-1`.",
              "pattern": "^mmx-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "grid_size": {
                  "description": "Number of modulation slots (grid dimensions)",
                  "enum": [
                    "1x1",
                    "2x2",
                    "3x3",
                    "4x4"
                  ],
                  "title": "Grid Size",
                  "type": "string"
                },
                "slot_10_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 10",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 10 Amount",
                  "type": "number"
                },
                "slot_10_dest": {
                  "description": "Modulation destination for slot 10. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 10 Dest",
                  "type": "string"
                },
                "slot_10_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 10",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 10 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_10_source": {
                  "description": "Modulation source for slot 10. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 10 Source",
                  "type": "string"
                },
                "slot_11_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 11",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 11 Amount",
                  "type": "number"
                },
                "slot_11_dest": {
                  "description": "Modulation destination for slot 11. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 11 Dest",
                  "type": "string"
                },
                "slot_11_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 11",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 11 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_11_source": {
                  "description": "Modulation source for slot 11. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 11 Source",
                  "type": "string"
                },
                "slot_12_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 12",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 12 Amount",
                  "type": "number"
                },
                "slot_12_dest": {
                  "description": "Modulation destination for slot 12. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 12 Dest",
                  "type": "string"
                },
                "slot_12_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 12",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 12 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_12_source": {
                  "description": "Modulation source for slot 12. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 12 Source",
                  "type": "string"
                },
                "slot_13_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 13",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 13 Amount",
                  "type": "number"
                },
                "slot_13_dest": {
                  "description": "Modulation destination for slot 13. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 13 Dest",
                  "type": "string"
                },
                "slot_13_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 13",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 13 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_13_source": {
                  "description": "Modulation source for slot 13. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 13 Source",
                  "type": "string"
                },
                "slot_14_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 14",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 14 Amount",
                  "type": "number"
                },
                "slot_14_dest": {
                  "description": "Modulation destination for slot 14. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 14 Dest",
                  "type": "string"
                },
                "slot_14_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 14",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 14 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_14_source": {
                  "description": "Modulation source for slot 14. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 14 Source",
                  "type": "string"
                },
                "slot_15_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 15",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 15 Amount",
                  "type": "number"
                },
                "slot_15_dest": {
                  "description": "Modulation destination for slot 15. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 15 Dest",
                  "type": "string"
                },
                "slot_15_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 15",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 15 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_15_source": {
                  "description": "Modulation source for slot 15. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 15 Source",
                  "type": "string"
                },
                "slot_16_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 16",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 16 Amount",
                  "type": "number"
                },
                "slot_16_dest": {
                  "description": "Modulation destination for slot 16. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 16 Dest",
                  "type": "string"
                },
                "slot_16_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 16",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 16 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_16_source": {
                  "description": "Modulation source for slot 16. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 16 Source",
                  "type": "string"
                },
                "slot_1_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 1",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 1 Amount",
                  "type": "number"
                },
                "slot_1_dest": {
                  "description": "Modulation destination for slot 1. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 1 Dest",
                  "type": "string"
                },
                "slot_1_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 1",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 1 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_1_source": {
                  "description": "Modulation source for slot 1. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 1 Source",
                  "type": "string"
                },
                "slot_2_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 2",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 2 Amount",
                  "type": "number"
                },
                "slot_2_dest": {
                  "description": "Modulation destination for slot 2. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 2 Dest",
                  "type": "string"
                },
                "slot_2_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 2",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 2 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_2_source": {
                  "description": "Modulation source for slot 2. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 2 Source",
                  "type": "string"
                },
                "slot_3_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 3",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 3 Amount",
                  "type": "number"
                },
                "slot_3_dest": {
                  "description": "Modulation destination for slot 3. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 3 Dest",
                  "type": "string"
                },
                "slot_3_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 3",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 3 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_3_source": {
                  "description": "Modulation source for slot 3. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 3 Source",
                  "type": "string"
                },
                "slot_4_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 4",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 4 Amount",
                  "type": "number"
                },
                "slot_4_dest": {
                  "description": "Modulation destination for slot 4. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 4 Dest",
                  "type": "string"
                },
                "slot_4_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 4",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 4 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_4_source": {
                  "description": "Modulation source for slot 4. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 4 Source",
                  "type": "string"
                },
                "slot_5_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 5",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 5 Amount",
                  "type": "number"
                },
                "slot_5_dest": {
                  "description": "Modulation destination for slot 5. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 5 Dest",
                  "type": "string"
                },
                "slot_5_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 5",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 5 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_5_source": {
                  "description": "Modulation source for slot 5. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 5 Source",
                  "type": "string"
                },
                "slot_6_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 6",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 6 Amount",
                  "type": "number"
                },
                "slot_6_dest": {
                  "description": "Modulation destination for slot 6. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 6 Dest",
                  "type": "string"
                },
                "slot_6_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 6",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 6 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_6_source": {
                  "description": "Modulation source for slot 6. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 6 Source",
                  "type": "string"
                },
                "slot_7_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 7",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 7 Amount",
                  "type": "number"
                },
                "slot_7_dest": {
                  "description": "Modulation destination for slot 7. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 7 Dest",
                  "type": "string"
                },
                "slot_7_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 7",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 7 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_7_source": {
                  "description": "Modulation source for slot 7. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 7 Source",
                  "type": "string"
                },
                "slot_8_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 8",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 8 Amount",
                  "type": "number"
                },
                "slot_8_dest": {
                  "description": "Modulation destination for slot 8. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 8 Dest",
                  "type": "string"
                },
                "slot_8_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 8",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 8 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_8_source": {
                  "description": "Modulation source for slot 8. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 8 Source",
                  "type": "string"
                },
                "slot_9_amount": {
                  "default": 0.0,
                  "description": "Modulation amount for slot 9",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Slot 9 Amount",
                  "type": "number"
                },
                "slot_9_dest": {
                  "description": "Modulation destination for slot 9. Accepts a full address string — any modulatable param on any module, e.g. \"flt-1.cutoff\" or \"spp-1.x\", a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; discover targets with get_instrument_automation_targets, set any address with set_parameter (string value), and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 9 Dest",
                  "type": "string"
                },
                "slot_9_enabled": {
                  "default": 1.0,
                  "description": "Enable/disable slot 9",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Slot 9 Enabled",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "slot_9_source": {
                  "description": "Modulation source for slot 9. Accepts a full address string — a module output like \"lfo-1.out\"/\"env-2.out\", a macro id (velocity, mod_wheel, …), a legacy id, or \"none\". The listed choices are only the common legacy shortcuts, NOT the full set; set any address with set_parameter (string value) and read the configured routing back with get_mod_matrix_routings.",
                  "title": "Slot 9 Source",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "mod_matrix",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Mod Matrix",
          "type": "object"
        },
        {
          "description": "Ring Mod (ring_mod) — Ring modulator — multiplies input with internal carrier oscillator",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `rng-1`.",
              "pattern": "^rng-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "carrier_freq": {
                  "default": 440.0,
                  "description": "Carrier oscillator frequency — unit: Hz",
                  "maximum": 20000.0,
                  "minimum": 0.10000000149011612,
                  "title": "Carrier Freq",
                  "type": "number"
                },
                "carrier_wave": {
                  "description": "Carrier oscillator waveform",
                  "enum": [
                    "sine",
                    "triangle",
                    "sawtooth",
                    "square",
                    "pulse",
                    "dsf_saw"
                  ],
                  "title": "Carrier Wave",
                  "type": "string"
                },
                "freq_ratio": {
                  "default": 0.5,
                  "description": "Carrier frequency ratio (0.25x to 4.0x)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Freq Ratio",
                  "type": "number"
                },
                "key_track": {
                  "default": 0.0,
                  "description": "Keyboard tracking (0=fixed, 1=track note) — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Key Track",
                  "type": "number"
                },
                "mix": {
                  "default": 0.5,
                  "description": "Dry/wet mix (0=dry, 1=ring mod) — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "ring_mod",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Ring Mod",
          "type": "object"
        },
        {
          "description": "Env Follower (envelope_follower) — Envelope follower — tracks input amplitude as a control signal",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `efl-1`.",
              "pattern": "^efl-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "attack": {
                  "default": 5.0,
                  "description": "How fast the follower rises — unit: ms",
                  "maximum": 100.0,
                  "minimum": 0.10000000149011612,
                  "title": "Attack",
                  "type": "number"
                },
                "release": {
                  "default": 50.0,
                  "description": "How fast the follower falls — unit: ms",
                  "maximum": 1000.0,
                  "minimum": 1.0,
                  "title": "Release",
                  "type": "number"
                },
                "sensitivity": {
                  "default": 0.5,
                  "description": "Output gain/sensitivity — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Sensitivity",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "envelope_follower",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Env Follower",
          "type": "object"
        },
        {
          "description": "Wavetable (wavetable_osc) — Wavetable oscillator with scannable waveform banks",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `wtb-1`.",
              "pattern": "^wtb-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "detune": {
                  "default": 0.0,
                  "description": "Detune in cents — unit: ct",
                  "maximum": 100.0,
                  "minimum": -100.0,
                  "title": "Detune",
                  "type": "number"
                },
                "glide_time": {
                  "default": 0.0,
                  "description": "Per-oscillator portamento time (0 = follow the voice glide) — unit: s",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Glide",
                  "type": "number"
                },
                "level": {
                  "default": 0.800000011920929,
                  "description": "Output level — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "octave": {
                  "default": 0.0,
                  "description": "Octave offset (-2 to +2) — unit: oct",
                  "maximum": 2.0,
                  "minimum": -2.0,
                  "title": "Octave",
                  "type": "number"
                },
                "position": {
                  "default": 0.0,
                  "description": "Scan position within the wavetable — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Position",
                  "type": "number"
                },
                "table": {
                  "description": "Wavetable bank selection",
                  "enum": [
                    "basic",
                    "harmonics",
                    "pwm",
                    "formant",
                    "digital",
                    "warm"
                  ],
                  "title": "Table",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "wavetable_osc",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Wavetable",
          "type": "object"
        },
        {
          "description": "MSEG (mseg) — Multi-Stage Envelope Generator with up to 16 segments",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `msg-1`.",
              "pattern": "^msg-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "loop": {
                  "default": 0.0,
                  "description": "Enable segment looping",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Loop",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "loop_end": {
                  "default": 1.0,
                  "description": "Last segment of the loop (0-15)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Loop End",
                  "type": "number"
                },
                "loop_start": {
                  "default": 0.0,
                  "description": "First segment of the loop (0-15)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Loop Start",
                  "type": "number"
                },
                "seg0_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 0 Curve",
                  "type": "number"
                },
                "seg0_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 0 Level",
                  "type": "number"
                },
                "seg0_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 0 Time",
                  "type": "number"
                },
                "seg10_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 10 Curve",
                  "type": "number"
                },
                "seg10_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 10 Level",
                  "type": "number"
                },
                "seg10_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 10 Time",
                  "type": "number"
                },
                "seg11_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 11 Curve",
                  "type": "number"
                },
                "seg11_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 11 Level",
                  "type": "number"
                },
                "seg11_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 11 Time",
                  "type": "number"
                },
                "seg12_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 12 Curve",
                  "type": "number"
                },
                "seg12_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 12 Level",
                  "type": "number"
                },
                "seg12_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 12 Time",
                  "type": "number"
                },
                "seg13_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 13 Curve",
                  "type": "number"
                },
                "seg13_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 13 Level",
                  "type": "number"
                },
                "seg13_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 13 Time",
                  "type": "number"
                },
                "seg14_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 14 Curve",
                  "type": "number"
                },
                "seg14_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 14 Level",
                  "type": "number"
                },
                "seg14_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 14 Time",
                  "type": "number"
                },
                "seg15_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 15 Curve",
                  "type": "number"
                },
                "seg15_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 15 Level",
                  "type": "number"
                },
                "seg15_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 15 Time",
                  "type": "number"
                },
                "seg1_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 1 Curve",
                  "type": "number"
                },
                "seg1_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 1 Level",
                  "type": "number"
                },
                "seg1_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 1 Time",
                  "type": "number"
                },
                "seg2_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 2 Curve",
                  "type": "number"
                },
                "seg2_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 2 Level",
                  "type": "number"
                },
                "seg2_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 2 Time",
                  "type": "number"
                },
                "seg3_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 3 Curve",
                  "type": "number"
                },
                "seg3_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 3 Level",
                  "type": "number"
                },
                "seg3_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 3 Time",
                  "type": "number"
                },
                "seg4_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 4 Curve",
                  "type": "number"
                },
                "seg4_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 4 Level",
                  "type": "number"
                },
                "seg4_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 4 Time",
                  "type": "number"
                },
                "seg5_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 5 Curve",
                  "type": "number"
                },
                "seg5_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 5 Level",
                  "type": "number"
                },
                "seg5_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 5 Time",
                  "type": "number"
                },
                "seg6_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 6 Curve",
                  "type": "number"
                },
                "seg6_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 6 Level",
                  "type": "number"
                },
                "seg6_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 6 Time",
                  "type": "number"
                },
                "seg7_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 7 Curve",
                  "type": "number"
                },
                "seg7_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 7 Level",
                  "type": "number"
                },
                "seg7_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 7 Time",
                  "type": "number"
                },
                "seg8_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 8 Curve",
                  "type": "number"
                },
                "seg8_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 8 Level",
                  "type": "number"
                },
                "seg8_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 8 Time",
                  "type": "number"
                },
                "seg9_curve": {
                  "default": 0.0,
                  "description": "Segment curve (-1 log, 0 linear, +1 exp)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Seg 9 Curve",
                  "type": "number"
                },
                "seg9_level": {
                  "default": 0.0,
                  "description": "Segment target level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seg 9 Level",
                  "type": "number"
                },
                "seg9_time": {
                  "default": 0.10000000149011612,
                  "description": "Segment duration in seconds — unit: s",
                  "maximum": 60.0,
                  "minimum": 0.0,
                  "title": "Seg 9 Time",
                  "type": "number"
                },
                "segments": {
                  "default": 4.0,
                  "description": "Number of active segments (1-16)",
                  "maximum": 16.0,
                  "minimum": 1.0,
                  "title": "Segments",
                  "type": "number"
                },
                "sustain_seg": {
                  "default": 2.0,
                  "description": "Segment index where envelope holds until gate off (0-15)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Sustain Seg",
                  "type": "number"
                },
                "time_scale": {
                  "default": 1.0,
                  "description": "Global time scale multiplier (0.01-10.0)",
                  "maximum": 10.0,
                  "minimum": 0.009999999776482582,
                  "title": "Time Scale",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "mseg",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "MSEG",
          "type": "object"
        },
        {
          "description": "Additive (additive_osc) — Additive synthesis with 32 harmonics",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `add-1`.",
              "pattern": "^add-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "brightness": {
                  "default": 0.699999988079071,
                  "description": "High harmonic presence",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Brightness",
                  "type": "number"
                },
                "glide_time": {
                  "default": 0.0,
                  "description": "Per-oscillator portamento time (0 = follow the voice glide) — unit: s",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Glide",
                  "type": "number"
                },
                "level": {
                  "default": 1.0,
                  "description": "Output level — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "odd_even": {
                  "default": 0.5,
                  "description": "Balance between odd and even harmonics",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Odd/Even",
                  "type": "number"
                },
                "randomize": {
                  "default": 0.10000000149011612,
                  "description": "Phase randomization on note-on",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Randomize",
                  "type": "number"
                },
                "stretch": {
                  "default": 0.0,
                  "description": "Spectral inharmonicity",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Stretch",
                  "type": "number"
                },
                "tilt": {
                  "default": 0.5,
                  "description": "Spectral rolloff per octave",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Tilt",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "additive_osc",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Additive",
          "type": "object"
        },
        {
          "description": "BBD Delay (bbd_delay) — Analog bucket-brigade delay emulation",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `bbd-1`.",
              "pattern": "^bbd-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "clock_noise": {
                  "default": 0.20000000298023224,
                  "description": "BBD clock noise amount",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Clock Noise",
                  "type": "number"
                },
                "feedback": {
                  "default": 0.5,
                  "description": "Feedback amount",
                  "maximum": 0.949999988079071,
                  "minimum": 0.0,
                  "title": "Feedback",
                  "type": "number"
                },
                "mix": {
                  "default": 0.5,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "time": {
                  "default": 0.30000001192092896,
                  "description": "Delay time — unit: s",
                  "maximum": 1.0,
                  "minimum": 0.009999999776482582,
                  "title": "Time",
                  "type": "number"
                },
                "tone": {
                  "default": 0.5,
                  "description": "Brightness of repeats",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Tone",
                  "type": "number"
                },
                "wow_flutter": {
                  "default": 0.30000001192092896,
                  "description": "Analog modulation depth",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Wow/Flutter",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "bbd_delay",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "BBD Delay",
          "type": "object"
        },
        {
          "description": "Mid/Side (mid_side) — Mid-Side stereo processing with width and gain controls",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `mds-1`.",
              "pattern": "^mds-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "mid_gain": {
                  "default": 0.0,
                  "description": "Mid channel gain — unit: dB",
                  "maximum": 12.0,
                  "minimum": -12.0,
                  "title": "Mid Gain",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "rotation": {
                  "default": 0.0,
                  "description": "Stereo field rotation (-180° to +180°)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Rotation",
                  "type": "number"
                },
                "side_gain": {
                  "default": 0.0,
                  "description": "Side channel gain — unit: dB",
                  "maximum": 12.0,
                  "minimum": -12.0,
                  "title": "Side Gain",
                  "type": "number"
                },
                "width": {
                  "default": 0.5,
                  "description": "Stereo width (0=mono, center=normal, max=extra wide)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Width",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "mid_side",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Mid/Side",
          "type": "object"
        },
        {
          "description": "Limiter (limiter) — Brickwall limiter with look-ahead",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `lmt-1`.",
              "pattern": "^lmt-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "ceiling": {
                  "default": -0.30000001192092896,
                  "description": "Output ceiling level — unit: dB",
                  "maximum": 0.0,
                  "minimum": -12.0,
                  "title": "Ceiling",
                  "type": "number"
                },
                "look_ahead": {
                  "default": 3.0,
                  "description": "Look-ahead time for peak detection — unit: ms",
                  "maximum": 5.0,
                  "minimum": 0.5,
                  "title": "Look-Ahead",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "release": {
                  "default": 100.0,
                  "description": "Gain recovery time — unit: ms",
                  "maximum": 500.0,
                  "minimum": 10.0,
                  "title": "Release",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "limiter",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Limiter",
          "type": "object"
        },
        {
          "description": "Euclidean (euclidean) — Euclidean rhythm sequencer — distributes pulses evenly across steps",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `euc-1`.",
              "pattern": "^euc-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "pulses": {
                  "default": 4.0,
                  "description": "Number of active pulses",
                  "maximum": 32.0,
                  "minimum": 0.0,
                  "title": "Pulses",
                  "type": "number"
                },
                "rotation": {
                  "default": 0.0,
                  "description": "Pattern rotation offset",
                  "maximum": 31.0,
                  "minimum": 0.0,
                  "title": "Rotation",
                  "type": "number"
                },
                "steps": {
                  "default": 16.0,
                  "description": "Number of steps in the pattern",
                  "maximum": 32.0,
                  "minimum": 1.0,
                  "title": "Steps",
                  "type": "number"
                },
                "swing": {
                  "default": 0.0,
                  "description": "Swing amount",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Swing",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "euclidean",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Euclidean",
          "type": "object"
        },
        {
          "description": "Turing Machine (turing_machine) — Shift-register random melody generator",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `tur-1`.",
              "pattern": "^tur-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "length": {
                  "default": 16.0,
                  "description": "Shift register length (8 or 16)",
                  "maximum": 16.0,
                  "minimum": 8.0,
                  "title": "Length",
                  "type": "number"
                },
                "mutation": {
                  "default": 0.5,
                  "description": "Mutation rate (0=locked, 0.5=evolving, 1=random)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mutation",
                  "type": "number"
                },
                "range": {
                  "default": 0.5,
                  "description": "Output pitch range",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Range",
                  "type": "number"
                },
                "scale": {
                  "description": "Scale quantization",
                  "enum": [
                    "chromatic",
                    "major",
                    "minor",
                    "pentatonic"
                  ],
                  "title": "Scale",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "turing_machine",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Turing Machine",
          "type": "object"
        },
        {
          "description": "Random Gates (random_gates) — Probabilistic gate generator with burst mode",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `rgt-1`.",
              "pattern": "^rgt-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "burst": {
                  "default": 0.10000000149011612,
                  "description": "Probability of triggering a burst",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Burst",
                  "type": "number"
                },
                "density": {
                  "default": 0.5,
                  "description": "Probability of gate trigger per step",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Density",
                  "type": "number"
                },
                "gate_len": {
                  "default": 0.5,
                  "description": "Gate length (short to long)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Gate Len",
                  "type": "number"
                },
                "seed": {
                  "default": 42.0,
                  "description": "Random seed for reproducibility",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seed",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "random_gates",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Random Gates",
          "type": "object"
        },
        {
          "description": "Keyboard Panner (keyboard_panner) — Note-based stereo positioning like a piano",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `kbp-1`.",
              "pattern": "^kbp-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "center": {
                  "default": 60.0,
                  "description": "Center note for panning (MIDI note)",
                  "maximum": 127.0,
                  "minimum": 0.0,
                  "title": "Center",
                  "type": "number"
                },
                "curve": {
                  "default": 0.0,
                  "description": "Pan curve shape (-1 to 1)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Curve",
                  "type": "number"
                },
                "invert": {
                  "description": "Invert panning direction",
                  "enum": [
                    "normal",
                    "inverted"
                  ],
                  "title": "Invert",
                  "type": "string"
                },
                "spread": {
                  "default": 0.5,
                  "description": "Stereo spread amount (0 = mono) — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Spread",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "keyboard_panner",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Keyboard Panner",
          "type": "object"
        },
        {
          "description": "Body Resonance (body_resonance) — Soundboard resonance simulation",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `bdy-1`.",
              "pattern": "^bdy-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "bright": {
                  "default": 0.5,
                  "description": "Brightness of resonance — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Bright",
                  "type": "number"
                },
                "freq": {
                  "default": 200.0,
                  "description": "Primary resonant frequency — unit: Hz",
                  "maximum": 2000.0,
                  "minimum": 50.0,
                  "title": "Freq",
                  "type": "number"
                },
                "mix": {
                  "default": 0.30000001192092896,
                  "description": "Wet/dry mix — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "resonance": {
                  "default": 0.5,
                  "description": "Resonance amount (Q factor) — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Resonance",
                  "type": "number"
                },
                "size": {
                  "default": 0.5,
                  "description": "Body size (affects frequency spread) — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Size",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "body_resonance",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Body Resonance",
          "type": "object"
        },
        {
          "description": "Mechanical Noise (mechanical_noise) — Key click and mechanical action sounds",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `mec-1`.",
              "pattern": "^mec-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "cutoff": {
                  "default": 3000.0,
                  "description": "Noise filter cutoff frequency — unit: Hz",
                  "maximum": 10000.0,
                  "minimum": 100.0,
                  "title": "Cutoff",
                  "type": "number"
                },
                "duration": {
                  "default": 10.0,
                  "description": "Noise burst duration — unit: ms",
                  "maximum": 100.0,
                  "minimum": 1.0,
                  "title": "Duration",
                  "type": "number"
                },
                "level": {
                  "default": 0.10000000149011612,
                  "description": "Output level — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "type": {
                  "description": "Type of mechanical noise",
                  "enum": [
                    "key_down",
                    "key_up",
                    "pedal",
                    "hammer"
                  ],
                  "title": "Type",
                  "type": "string"
                },
                "vel_sens": {
                  "default": 0.5,
                  "description": "Velocity sensitivity — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Vel Sens",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "mechanical_noise",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Mechanical Noise",
          "type": "object"
        },
        {
          "description": "Granular (granular_osc) — Granular synthesis with 32 concurrent grains",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `grn-1`.",
              "pattern": "^grn-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "density": {
                  "default": 0.5,
                  "description": "Grain trigger rate (0 = sparse, 1 = dense)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Density",
                  "type": "number"
                },
                "freeze": {
                  "default": 0.0,
                  "description": "Freeze source buffer playback (0 = off, 1 = on)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Freeze",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "glide_time": {
                  "default": 0.0,
                  "description": "Per-oscillator portamento time (0 = follow the voice glide) — unit: s",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Glide",
                  "type": "number"
                },
                "grain_size": {
                  "default": 50.0,
                  "description": "Length of each grain in milliseconds — unit: ms",
                  "maximum": 500.0,
                  "minimum": 5.0,
                  "title": "Grain Size",
                  "type": "number"
                },
                "level": {
                  "default": 1.0,
                  "description": "Output level — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "pan_spread": {
                  "default": 0.0,
                  "description": "Random stereo pan per grain (0 = mono, 1 = full)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Pan Spread",
                  "type": "number"
                },
                "pitch_spread": {
                  "default": 0.0,
                  "description": "Random pitch variation per grain",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Pitch Spread",
                  "type": "number"
                },
                "pos_spread": {
                  "default": 0.10000000149011612,
                  "description": "Random offset around the read position per grain",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Pos Spread",
                  "type": "number"
                },
                "position": {
                  "default": 0.0,
                  "description": "Read position within the source buffer (0 = start, 1 = end)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Position",
                  "type": "number"
                },
                "source": {
                  "description": "Source waveform used to fill the grain buffer",
                  "enum": [
                    "saw",
                    "sine",
                    "square",
                    "triangle",
                    "noise"
                  ],
                  "title": "Source",
                  "type": "string"
                },
                "window": {
                  "description": "Grain envelope shape",
                  "enum": [
                    "hann",
                    "gaussian",
                    "trapezoid"
                  ],
                  "title": "Window",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "granular_osc",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Granular",
          "type": "object"
        },
        {
          "description": "Convolver (convolver) — Convolution reverb with generated impulse responses",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `cnv-1`.",
              "pattern": "^cnv-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "brightness": {
                  "default": 0.800000011920929,
                  "description": "High frequency content of reverb tail",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Brightness",
                  "type": "number"
                },
                "decay": {
                  "default": 1.0,
                  "description": "IR tail length trim",
                  "maximum": 1.0,
                  "minimum": 0.10000000149011612,
                  "title": "Decay",
                  "type": "number"
                },
                "dynamic": {
                  "default": 0.0,
                  "description": "Dynamic convolution amount (amplitude-dependent IR)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Dynamic",
                  "type": "number"
                },
                "ir_type": {
                  "description": "Impulse response type (Plate, Room, Spring, Hall)",
                  "enum": [
                    "plate",
                    "room",
                    "spring",
                    "hall"
                  ],
                  "title": "IR Type",
                  "type": "string"
                },
                "mix": {
                  "default": 0.30000001192092896,
                  "description": "Dry/wet mix — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "pre_delay": {
                  "default": 0.0,
                  "description": "Time before reverb onset — unit: ms",
                  "maximum": 200.0,
                  "minimum": 0.0,
                  "title": "Pre-Delay",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "convolver",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Convolver",
          "type": "object"
        },
        {
          "description": "Phase Vocoder (phase_vocoder) — Spectral pitch shifting and freeze",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `pvc-1`.",
              "pattern": "^pvc-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "fft_size": {
                  "description": "FFT window size (larger = better frequency resolution)",
                  "enum": [
                    "512",
                    "1024",
                    "2048",
                    "4096"
                  ],
                  "title": "FFT Size",
                  "type": "string"
                },
                "freeze": {
                  "default": 0.0,
                  "description": "Freeze current spectrum (0 = off, 1 = on)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Freeze",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "pitch_shift": {
                  "default": 0.0,
                  "description": "Pitch shift in semitones — unit: st",
                  "maximum": 24.0,
                  "minimum": -24.0,
                  "title": "Pitch Shift",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "phase_vocoder",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Phase Vocoder",
          "type": "object"
        },
        {
          "description": "Kinetic Mod (kinetic_modulator) — Easing curve modulation generator",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `kin-1`.",
              "pattern": "^kin-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "bipolar": {
                  "default": 0.0,
                  "description": "Output range (0 = 0..1, 1 = -1..+1)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Bipolar",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "curve": {
                  "description": "Easing curve shape",
                  "enum": [
                    "linear",
                    "quad_out",
                    "cubic_out",
                    "quart_out",
                    "quint_out",
                    "expo_out",
                    "circ_out",
                    "back_out",
                    "elastic_out",
                    "bounce_out"
                  ],
                  "title": "Curve",
                  "type": "string"
                },
                "duration": {
                  "default": 0.5,
                  "description": "Length of one modulation sweep — unit: s",
                  "maximum": 10.0,
                  "minimum": 0.009999999776482582,
                  "title": "Duration",
                  "type": "number"
                },
                "loop_mode": {
                  "description": "Loop behaviour (one-shot, loop, ping-pong)",
                  "enum": [
                    "one_shot",
                    "loop",
                    "ping_pong"
                  ],
                  "title": "Loop Mode",
                  "type": "string"
                },
                "overshoot": {
                  "default": 0.5,
                  "description": "Overshoot amount for back/elastic curves",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Overshoot",
                  "type": "number"
                },
                "retrigger": {
                  "default": 1.0,
                  "description": "Restart on gate (0 = off, 1 = on)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Retrigger",
                  "type": [
                    "number",
                    "boolean"
                  ]
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "kinetic_modulator",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Kinetic Mod",
          "type": "object"
        },
        {
          "description": "Sig Mon (signal_monitor) — Signal Monitor — inline waveform display (pass-through)",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `smn-1`.",
              "pattern": "^smn-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "freeze": {
                  "default": 0.0,
                  "description": "Freeze the display (stop capturing)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Freeze",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "gain": {
                  "default": 1.0,
                  "description": "Vertical gain",
                  "maximum": 10.0,
                  "minimum": 0.10000000149011612,
                  "title": "Gain",
                  "type": "number"
                },
                "time": {
                  "default": 0.009999999776482582,
                  "description": "Time scale (zoom) — unit: s",
                  "maximum": 0.10000000149011612,
                  "minimum": 0.0010000000474974513,
                  "title": "Time",
                  "type": "number"
                },
                "trig": {
                  "default": 0.5,
                  "description": "Trigger level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Trig",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "signal_monitor",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Sig Mon",
          "type": "object"
        },
        {
          "description": "Freq Shifter (frequency_shifter) — Bode frequency shifter — shifts all frequencies by a fixed Hz amount",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `fsf-1`.",
              "pattern": "^fsf-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "mode": {
                  "default": 0.0,
                  "description": "0=Up shift, 0.5=Down shift, 1.0=Stereo (up L, down R)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mode",
                  "type": "number"
                },
                "shift": {
                  "default": 0.0,
                  "description": "Frequency shift amount in Hz — unit: Hz",
                  "maximum": 1000.0,
                  "minimum": -1000.0,
                  "title": "Shift",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "frequency_shifter",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Freq Shifter",
          "type": "object"
        },
        {
          "description": "Vector Mixer (vector_mixer) — 4-corner XY vector crossfader for vector synthesis",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `vec-1`.",
              "pattern": "^vec-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "x": {
                  "default": 0.0,
                  "description": "X position (-1=left, +1=right)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "X",
                  "type": "number"
                },
                "y": {
                  "default": 0.0,
                  "description": "Y position (-1=bottom, +1=top)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Y",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "vector_mixer",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Vector Mixer",
          "type": "object"
        },
        {
          "description": "LA Synth (la_synth) — Linear Arithmetic synthesis — attack transient crossfaded to sustain input",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `las-1`.",
              "pattern": "^las-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "attack_level": {
                  "default": 0.699999988079071,
                  "description": "Level of attack transient",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Attack Level",
                  "type": "number"
                },
                "attack_time": {
                  "default": 30.0,
                  "description": "Duration of attack transient — unit: ms",
                  "maximum": 200.0,
                  "minimum": 1.0,
                  "title": "Attack Time",
                  "type": "number"
                },
                "attack_type": {
                  "default": 0.0,
                  "description": "0=Click, 0.33=Noise burst, 0.66=Pluck, 1.0=Hammer",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Attack Type",
                  "type": "number"
                },
                "brightness": {
                  "default": 0.5,
                  "description": "Brightness filter on attack transient",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Brightness",
                  "type": "number"
                },
                "x_fade_time": {
                  "default": 50.0,
                  "description": "Crossfade time from attack to sustain — unit: ms",
                  "maximum": 500.0,
                  "minimum": 1.0,
                  "title": "X-Fade Time",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "la_synth",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "LA Synth",
          "type": "object"
        },
        {
          "description": "Pitch Tracker (pitch_tracker) — Autocorrelation pitch detector — outputs CV and gate from audio input",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `ptr-1`.",
              "pattern": "^ptr-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "max_freq": {
                  "default": 2000.0,
                  "description": "Maximum trackable frequency — unit: Hz",
                  "maximum": 8000.0,
                  "minimum": 200.0,
                  "title": "Max Freq",
                  "type": "number"
                },
                "min_freq": {
                  "default": 50.0,
                  "description": "Minimum trackable frequency — unit: Hz",
                  "maximum": 500.0,
                  "minimum": 20.0,
                  "title": "Min Freq",
                  "type": "number"
                },
                "sensitivity": {
                  "default": 0.5,
                  "description": "Gate threshold — how confident the detection must be",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Sensitivity",
                  "type": "number"
                },
                "smoothing": {
                  "default": 0.30000001192092896,
                  "description": "Output smoothing amount",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Smoothing",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "pitch_tracker",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Pitch Tracker",
          "type": "object"
        },
        {
          "description": "Ensemble Chorus (ensemble_chorus) — Juno-style BBD ensemble chorus with stereo width",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `enc-1`.",
              "pattern": "^enc-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "base_delay": {
                  "default": 12.0,
                  "description": "Base delay time in ms — unit: ms",
                  "maximum": 20.0,
                  "minimum": 5.0,
                  "title": "Base Delay",
                  "type": "number"
                },
                "depth": {
                  "default": 1.2000000476837158,
                  "description": "Modulation depth in ms — unit: ms",
                  "maximum": 2.5,
                  "minimum": 0.20000000298023224,
                  "title": "Depth",
                  "type": "number"
                },
                "mix": {
                  "default": 0.5,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "noise": {
                  "default": 0.10000000149011612,
                  "description": "BBD clock noise amount",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Noise",
                  "type": "number"
                },
                "rate": {
                  "default": 0.6000000238418579,
                  "description": "LFO rate — unit: Hz",
                  "maximum": 1.5,
                  "minimum": 0.20000000298023224,
                  "title": "Rate",
                  "type": "number"
                },
                "stereo_width": {
                  "default": 1.0,
                  "description": "Stereo spread via mid/side",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Stereo Width",
                  "type": "number"
                },
                "tone": {
                  "default": 0.4000000059604645,
                  "description": "Lowpass tone in delay path",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Tone",
                  "type": "number"
                },
                "voices": {
                  "default": 2.0,
                  "description": "Number of chorus voices (2 or 3)",
                  "maximum": 3.0,
                  "minimum": 2.0,
                  "title": "Voices",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "ensemble_chorus",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Ensemble Chorus",
          "type": "object"
        },
        {
          "description": "Shimmer Reverb (shimmer_reverb) — FDN reverb with pitch-shifted feedback",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `shr-1`.",
              "pattern": "^shr-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "damping": {
                  "default": 0.4000000059604645,
                  "description": "High-frequency absorption in feedback path",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Damping",
                  "type": "number"
                },
                "decay": {
                  "default": 0.699999988079071,
                  "description": "Reverb tail length (feedback gain)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Decay",
                  "type": "number"
                },
                "mix": {
                  "default": 0.4000000059604645,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "pitch": {
                  "default": 12.0,
                  "description": "Pitch shift in semitones — unit: st",
                  "maximum": 24.0,
                  "minimum": -12.0,
                  "title": "Pitch",
                  "type": "number"
                },
                "pre_delay": {
                  "default": 0.0,
                  "description": "Initial delay before reverb onset — unit: s",
                  "maximum": 0.5,
                  "minimum": 0.0,
                  "title": "Pre-Delay",
                  "type": "number"
                },
                "room_size": {
                  "default": 0.6000000238418579,
                  "description": "Size of the virtual room (scales delay times)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Room Size",
                  "type": "number"
                },
                "shimmer": {
                  "default": 0.3499999940395355,
                  "description": "Amount of pitch-shifted feedback",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Shimmer",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "shimmer_reverb",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Shimmer Reverb",
          "type": "object"
        },
        {
          "description": "Granular FX (granular_fx) — Input-driven granular cloud processor",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `gfx-1`.",
              "pattern": "^gfx-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "buffer": {
                  "default": 2.0,
                  "description": "Capture buffer length — unit: s",
                  "maximum": 4.0,
                  "minimum": 0.5,
                  "title": "Buffer",
                  "type": "number"
                },
                "density": {
                  "default": 0.6000000238418579,
                  "description": "Grain trigger rate (0 = sparse, 1 = dense)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Density",
                  "type": "number"
                },
                "freeze": {
                  "default": 0.0,
                  "description": "Stop writing to buffer",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Freeze",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "grain_size": {
                  "default": 40.0,
                  "description": "Length of each grain in milliseconds — unit: ms",
                  "maximum": 120.0,
                  "minimum": 10.0,
                  "title": "Grain Size",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "pan_spread": {
                  "default": 0.5,
                  "description": "Random stereo pan per grain (0 = mono, 1 = full)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Pan Spread",
                  "type": "number"
                },
                "pitch_spread": {
                  "default": 0.20000000298023224,
                  "description": "Random pitch variation per grain",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Pitch Spread",
                  "type": "number"
                },
                "pos_spread": {
                  "default": 0.30000001192092896,
                  "description": "Random offset around the read position per grain",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Pos Spread",
                  "type": "number"
                },
                "position": {
                  "default": 0.5,
                  "description": "Read offset into the capture buffer (0 = newest, 1 = oldest)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Position",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "granular_fx",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Granular FX",
          "type": "object"
        },
        {
          "description": "Spectral Blur (spectral_blur) — STFT-based spectral smearing effect",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `sbl-1`.",
              "pattern": "^sbl-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "blur_freq": {
                  "default": 0.30000001192092896,
                  "description": "Spectral smoothing amount",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Blur Freq",
                  "type": "number"
                },
                "blur_time": {
                  "default": 0.699999988079071,
                  "description": "Temporal smoothing amount",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Blur Time",
                  "type": "number"
                },
                "fft_size": {
                  "description": "FFT window size",
                  "enum": [
                    "512",
                    "1024",
                    "2048",
                    "4096"
                  ],
                  "title": "FFT Size",
                  "type": "string"
                },
                "freeze": {
                  "default": 0.0,
                  "description": "Hold current spectrum",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Freeze",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "spectral_blur",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Spectral Blur",
          "type": "object"
        },
        {
          "description": "Modal Resonator (modal_resonator) — Bank of tuned bandpass resonators",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `mdr-1`.",
              "pattern": "^mdr-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "base_note": {
                  "default": 60.0,
                  "description": "Base MIDI note for fundamental",
                  "maximum": 96.0,
                  "minimum": 24.0,
                  "title": "Base Note",
                  "type": "number"
                },
                "brightness": {
                  "default": 0.5,
                  "description": "High mode level and Q",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Brightness",
                  "type": "number"
                },
                "decay": {
                  "default": 0.6000000238418579,
                  "description": "Resonance decay (Q)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Decay",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "modes": {
                  "default": 8.0,
                  "description": "Number of resonant modes",
                  "maximum": 16.0,
                  "minimum": 4.0,
                  "title": "Modes",
                  "type": "number"
                },
                "spread": {
                  "default": 0.30000001192092896,
                  "description": "Inharmonicity/detune spread",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Spread",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "modal_resonator",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Modal Resonator",
          "type": "object"
        },
        {
          "description": "Reverse/Gate Reverb (reverse_gate_reverb) — Buffer capture with reverse/gate/stutter playback",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `rgr-1`.",
              "pattern": "^rgr-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "gate_time": {
                  "default": 120.0,
                  "description": "Envelope gate/decay time — unit: ms",
                  "maximum": 500.0,
                  "minimum": 20.0,
                  "title": "Gate Time",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "mode": {
                  "description": "Playback mode",
                  "enum": [
                    "reverse",
                    "gate",
                    "stutter"
                  ],
                  "title": "Mode",
                  "type": "string"
                },
                "threshold": {
                  "default": -24.0,
                  "description": "Threshold for trigger detection — unit: dB",
                  "maximum": 0.0,
                  "minimum": -60.0,
                  "title": "Threshold",
                  "type": "number"
                },
                "trigger": {
                  "description": "Trigger mode",
                  "enum": [
                    "periodic",
                    "threshold"
                  ],
                  "title": "Trigger",
                  "type": "string"
                },
                "window": {
                  "default": 600.0,
                  "description": "Capture window length — unit: ms",
                  "maximum": 2000.0,
                  "minimum": 100.0,
                  "title": "Window",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "reverse_gate_reverb",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Reverse/Gate Reverb",
          "type": "object"
        },
        {
          "description": "Fractal Osc (fractal_osc) — Weierstrass fractal oscillator with stereo spread",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `frc-1`.",
              "pattern": "^frc-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "dispersion": {
                  "default": 0.0,
                  "description": "Phase offset per partial for crest factor control",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Dispersion",
                  "type": "number"
                },
                "glide_time": {
                  "default": 0.0,
                  "description": "Per-oscillator portamento time (0 = follow the voice glide) — unit: s",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Glide",
                  "type": "number"
                },
                "level": {
                  "default": 1.0,
                  "description": "Output level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "roughness": {
                  "default": 0.5,
                  "description": "Amplitude scaling per partial (fractal roughness)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Roughness",
                  "type": "number"
                },
                "spacing": {
                  "default": 0.10999999940395355,
                  "description": "Frequency ratio between partials (1.01–10.0)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Spacing",
                  "type": "number"
                },
                "spread": {
                  "default": 0.5,
                  "description": "Stereo spread (even partials left, odd right)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Spread",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "fractal_osc",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Fractal Osc",
          "type": "object"
        },
        {
          "description": "Sampler (sampler) — Sample playback module — plays WAV samples as instruments",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `sam-1`.",
              "pattern": "^sam-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "direction": {
                  "description": "Playback direction",
                  "enum": [
                    "forward",
                    "reverse",
                    "ping_pong"
                  ],
                  "title": "Direction",
                  "type": "string"
                },
                "fine_tune": {
                  "default": 0.0,
                  "description": "Fine-tune in cents — unit: ct",
                  "maximum": 100.0,
                  "minimum": -100.0,
                  "title": "Fine Tune",
                  "type": "number"
                },
                "level": {
                  "default": 0.800000011920929,
                  "description": "Output level — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "pitch_tracking": {
                  "default": 1.0,
                  "description": "Follow MIDI note pitch",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Pitch Track",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "play_mode": {
                  "description": "How the sample responds to note-on/off",
                  "enum": [
                    "one_shot",
                    "sustain",
                    "loop"
                  ],
                  "title": "Play Mode",
                  "type": "string"
                },
                "sample_select": {
                  "description": "Assigned sample (selected via the sample picker)",
                  "properties": {
                    "sample_id": {
                      "minimum": 0,
                      "type": "integer"
                    }
                  },
                  "required": [
                    "sample_id"
                  ],
                  "title": "Sample",
                  "type": "object"
                },
                "start_offset": {
                  "default": 0.0,
                  "description": "Playback start offset (0.0 = beginning, 1.0 = end) — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Start",
                  "type": "number"
                },
                "velocity_sensitivity": {
                  "default": 1.0,
                  "description": "Velocity sensitivity — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Vel Sens",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "sampler",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Sampler",
          "type": "object"
        },
        {
          "description": "Audio Input (audio_input) — Live audio input from microphone or line-in",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `ain-1`.",
              "pattern": "^ain-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "level": {
                  "default": 1.0,
                  "description": "Input level — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "audio_input",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Audio Input",
          "type": "object"
        },
        {
          "description": "Ladder Filter (ladder_filter) — 24dB/oct Moog-style ladder filter",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `ldr-1`.",
              "pattern": "^ldr-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "cutoff": {
                  "default": 1000.0,
                  "description": "Cutoff frequency — unit: Hz",
                  "maximum": 20000.0,
                  "minimum": 20.0,
                  "title": "Cutoff",
                  "type": "number"
                },
                "drive": {
                  "default": 1.0,
                  "description": "Saturation amount (soft clipping above 1.0)",
                  "maximum": 4.0,
                  "minimum": 0.5,
                  "title": "Drive",
                  "type": "number"
                },
                "resonance": {
                  "default": 0.0,
                  "description": "Filter resonance",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Resonance",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "ladder_filter",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Ladder Filter",
          "type": "object"
        },
        {
          "description": "Drift Generator (drift_generator) — Smooth random modulation for analog-style drift",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `drf-1`.",
              "pattern": "^drf-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "depth": {
                  "default": 0.5,
                  "description": "Output amplitude",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Depth",
                  "type": "number"
                },
                "rate": {
                  "default": 0.20000000298023224,
                  "description": "How fast the drift wanders — unit: Hz",
                  "maximum": 5.0,
                  "minimum": 0.009999999776482582,
                  "title": "Rate",
                  "type": "number"
                },
                "smoothness": {
                  "default": 0.699999988079071,
                  "description": "Higher = smoother transitions",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Smooth",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "drift_generator",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Drift Generator",
          "type": "object"
        },
        {
          "description": "Chaotic Osc (chaotic_osc) — Rössler/Lorenz chaotic systems for complex modulation",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `cha-1`.",
              "pattern": "^cha-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "chaos": {
                  "default": 0.5,
                  "description": "System parameter (low=periodic, high=chaotic)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Chaos",
                  "type": "number"
                },
                "depth": {
                  "default": 1.0,
                  "description": "Output amplitude",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Depth",
                  "type": "number"
                },
                "rate": {
                  "default": 1.0,
                  "description": "Iteration speed — unit: Hz",
                  "maximum": 20.0,
                  "minimum": 0.009999999776482582,
                  "title": "Rate",
                  "type": "number"
                },
                "system": {
                  "description": "Chaotic dynamical system",
                  "enum": [
                    "rossler",
                    "lorenz"
                  ],
                  "title": "System",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "chaotic_osc",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Chaotic Osc",
          "type": "object"
        },
        {
          "description": "Formant Filter (formant_filter) — Vowel-shaping filter (A-E-I-O-U morphing)",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `fmt-1`.",
              "pattern": "^fmt-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "cutoff": {
                  "default": 1000.0,
                  "description": "Scales formant frequencies — unit: Hz",
                  "maximum": 5000.0,
                  "minimum": 200.0,
                  "title": "Cutoff",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "resonance": {
                  "default": 0.5,
                  "description": "Formant sharpness",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Resonance",
                  "type": "number"
                },
                "vowel": {
                  "default": 0.0,
                  "description": "Vowel morph (A → E → I → O → U)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Vowel",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "formant_filter",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Formant Filter",
          "type": "object"
        },
        {
          "description": "Fooglers (fooglers) — Micro delay-line weird synthesis (physical modeling)",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `fog-1`.",
              "pattern": "^fog-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "damping": {
                  "default": 0.30000001192092896,
                  "description": "High-frequency damping",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Damping",
                  "type": "number"
                },
                "feedback": {
                  "default": 0.800000011920929,
                  "description": "Feedback amount (sustain)",
                  "maximum": 0.9900000095367432,
                  "minimum": 0.0,
                  "title": "Feedback",
                  "type": "number"
                },
                "level": {
                  "default": 0.5,
                  "description": "Output level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "tap1": {
                  "default": 0.30000001192092896,
                  "description": "First tap position in delay line",
                  "maximum": 1.0,
                  "minimum": 0.009999999776482582,
                  "title": "Tap 1",
                  "type": "number"
                },
                "tap2": {
                  "default": 0.699999988079071,
                  "description": "Second tap position in delay line",
                  "maximum": 1.0,
                  "minimum": 0.009999999776482582,
                  "title": "Tap 2",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "fooglers",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Fooglers",
          "type": "object"
        },
        {
          "description": "Beat Detector (beat_detector) — Envelope-based beat/transient detector with gate output",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `btd-1`.",
              "pattern": "^btd-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "filter_freq": {
                  "default": 150.0,
                  "description": "Envelope lowpass frequency — unit: Hz",
                  "maximum": 1000.0,
                  "minimum": 20.0,
                  "title": "Filter",
                  "type": "number"
                },
                "hold_time": {
                  "default": 50.0,
                  "description": "Gate hold time — unit: ms",
                  "maximum": 500.0,
                  "minimum": 1.0,
                  "title": "Hold",
                  "type": "number"
                },
                "sensitivity": {
                  "default": 0.5,
                  "description": "Detection threshold",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Sensitivity",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "beat_detector",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Beat Detector",
          "type": "object"
        },
        {
          "description": "PADsynth (pad_synth) — Lush pad synthesis using the PADsynth algorithm",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `pad-1`.",
              "pattern": "^pad-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "bandwidth": {
                  "default": 0.4000000059604645,
                  "description": "Harmonic bandwidth spread (wider = lusher)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Bandwidth",
                  "type": "number"
                },
                "base_freq": {
                  "default": 261.6300048828125,
                  "description": "Base frequency for wavetable generation — unit: Hz",
                  "maximum": 2000.0,
                  "minimum": 20.0,
                  "title": "Base Freq",
                  "type": "number"
                },
                "detune": {
                  "default": 0.0,
                  "description": "Random per-harmonic frequency shift",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Detune",
                  "type": "number"
                },
                "level": {
                  "default": 0.800000011920929,
                  "description": "Output level — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "tilt": {
                  "default": 0.5,
                  "description": "Harmonic rolloff per octave",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Tilt",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "pad_synth",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "PADsynth",
          "type": "object"
        },
        {
          "description": "AM Formant (am_formant) — AM formantic synthesis (vocal-like tones)",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `amf-1`.",
              "pattern": "^amf-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "carrier_ratio": {
                  "default": 0.5,
                  "description": "Scales carrier (formant) frequencies",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Carrier",
                  "type": "number"
                },
                "depth": {
                  "default": 0.800000011920929,
                  "description": "AM modulation depth",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Depth",
                  "type": "number"
                },
                "level": {
                  "default": 0.800000011920929,
                  "description": "Output level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "vowel": {
                  "default": 0.0,
                  "description": "Vowel morph (A -> E -> I -> O -> U)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Vowel",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "am_formant",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "AM Formant",
          "type": "object"
        },
        {
          "description": "Tilt EQ (tilt_eq) — Single-knob spectral tilt equalizer",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `teq-1`.",
              "pattern": "^teq-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "center_freq": {
                  "default": 800.0,
                  "description": "Pivot frequency — unit: Hz",
                  "maximum": 8000.0,
                  "minimum": 100.0,
                  "title": "Center",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "tilt": {
                  "default": 0.0,
                  "description": "Spectral tilt (-1=dark, 0=flat, +1=bright)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Tilt",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "tilt_eq",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Tilt EQ",
          "type": "object"
        },
        {
          "description": "Univibe (univibe) — Classic Univox Univibe phaser/vibrato emulation",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `uvb-1`.",
              "pattern": "^uvb-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "depth": {
                  "default": 0.699999988079071,
                  "description": "Modulation depth",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Depth",
                  "type": "number"
                },
                "feedback": {
                  "default": 0.30000001192092896,
                  "description": "Feedback amount",
                  "maximum": 0.949999988079071,
                  "minimum": 0.0,
                  "title": "Feedback",
                  "type": "number"
                },
                "mix": {
                  "default": 0.5,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "rate": {
                  "default": 2.0,
                  "description": "LFO speed — unit: Hz",
                  "maximum": 10.0,
                  "minimum": 0.10000000149011612,
                  "title": "Rate",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "univibe",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Univibe",
          "type": "object"
        },
        {
          "description": "Crossover Splitter (crossover_splitter) — Phase-coherent frequency band splitter with independent gain",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `cxo-1`.",
              "pattern": "^cxo-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "frequency": {
                  "default": 1000.0,
                  "description": "Crossover frequency — unit: Hz",
                  "maximum": 20000.0,
                  "minimum": 20.0,
                  "title": "Frequency",
                  "type": "number"
                },
                "high_gain": {
                  "default": 1.0,
                  "description": "High band gain",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "High Gain",
                  "type": "number"
                },
                "low_gain": {
                  "default": 1.0,
                  "description": "Low band gain",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Low Gain",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "crossover_splitter",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Crossover Splitter",
          "type": "object"
        },
        {
          "description": "LPC Vocoder (vocoder) — LPC spectral envelope vocoder effect",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `vcd-1`.",
              "pattern": "^vcd-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "order": {
                  "default": 0.5,
                  "description": "LPC analysis order (4-32)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Order",
                  "type": "number"
                },
                "window_size": {
                  "default": 20.0,
                  "description": "Analysis window size — unit: ms",
                  "maximum": 50.0,
                  "minimum": 5.0,
                  "title": "Window",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "vocoder",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "LPC Vocoder",
          "type": "object"
        },
        {
          "description": "Transient Shaper (transient_shaper) — Differential-envelope transient designer: independent attack/sustain gain shaping",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `tsh-1`.",
              "pattern": "^tsh-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "attack": {
                  "default": 0.0,
                  "description": "Onset gain in dB (+ adds punch, − softens the attack) — unit: dB",
                  "maximum": 24.0,
                  "minimum": -24.0,
                  "title": "Attack",
                  "type": "number"
                },
                "mix": {
                  "default": 1.0,
                  "description": "Dry/wet mix",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Mix",
                  "type": "number"
                },
                "sensitivity": {
                  "default": 1.0,
                  "description": "Overall effect amount (0 = off, 1 = full shaping)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Sensitivity",
                  "type": "number"
                },
                "sustain": {
                  "default": 0.0,
                  "description": "Body/tail gain in dB (+ lengthens/fills, − tightens/gates) — unit: dB",
                  "maximum": 24.0,
                  "minimum": -24.0,
                  "title": "Sustain",
                  "type": "number"
                },
                "window": {
                  "default": 10.0,
                  "description": "Transient detection window in ms — how fast onsets are detected — unit: ms",
                  "maximum": 50.0,
                  "minimum": 1.0,
                  "title": "Window",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "transient_shaper",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Transient Shaper",
          "type": "object"
        },
        {
          "description": "Voice Synth (voice_synth) — Physically-inspired singing voice / choir (glottal source → formants)",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `vox-1`.",
              "pattern": "^vox-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "breathiness": {
                  "default": 0.0,
                  "description": "Aspiration / breath noise amount",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Breathiness",
                  "type": "number"
                },
                "formant_shift": {
                  "default": 0.5,
                  "description": "Vocal-tract length: <0.5 longer/darker, >0.5 shorter/brighter",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Formant Shift",
                  "type": "number"
                },
                "level": {
                  "default": 0.800000011920929,
                  "description": "Output level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "open_quotient": {
                  "default": 0.5,
                  "description": "Glottal open quotient: low = pressed, high = soft/breathy",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Open Quotient",
                  "type": "number"
                },
                "tilt": {
                  "default": 0.0,
                  "description": "Spectral tilt: 0 bright, 1 dark (soft glottal closure)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Tilt",
                  "type": "number"
                },
                "unison_detune": {
                  "default": 15.0,
                  "description": "Pitch spread across unison voices — unit: ct",
                  "maximum": 50.0,
                  "minimum": 0.0,
                  "title": "Unison Detune",
                  "type": "number"
                },
                "unison_spread": {
                  "default": 0.699999988079071,
                  "description": "Stereo width of the choir (0 = mono, 1 = full)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Unison Spread",
                  "type": "number"
                },
                "unison_voices": {
                  "default": 0.0,
                  "description": "Choir size: 1 (solo) to 16 decorrelated voices",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Unison Voices",
                  "type": "number"
                },
                "vibrato_depth": {
                  "default": 0.0,
                  "description": "Vibrato depth in cents — unit: ct",
                  "maximum": 100.0,
                  "minimum": 0.0,
                  "title": "Vibrato Depth",
                  "type": "number"
                },
                "vibrato_rate": {
                  "default": 5.5,
                  "description": "Vibrato LFO rate — unit: Hz",
                  "maximum": 12.0,
                  "minimum": 0.0,
                  "title": "Vibrato Rate",
                  "type": "number"
                },
                "vowel": {
                  "default": 0.0,
                  "description": "Vowel morph (A → E → I → O → U)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Vowel",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "voice_synth",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Voice Synth",
          "type": "object"
        },
        {
          "description": "Vocal Tract (vocal_tract) — Articulatory Kelly–Lochbaum waveguide voice (speech-grade)",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `vtr-1`.",
              "pattern": "^vtr-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "breathiness": {
                  "default": 0.10000000149011612,
                  "description": "Aspiration / breath noise at the glottis",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Breathiness",
                  "type": "number"
                },
                "constriction": {
                  "default": 0.30000001192092896,
                  "description": "Constriction amount (0 = open tube, 1 = tight)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Constriction",
                  "type": "number"
                },
                "length": {
                  "default": 0.5,
                  "description": "Tract length / voice type (0 = short → soprano/child, 0.5 = neutral, 1 = long → bass). Scales all formants.",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Length",
                  "type": "number"
                },
                "level": {
                  "default": 0.800000011920929,
                  "description": "Output level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "lips": {
                  "default": 0.5,
                  "description": "Lip aperture (0 = rounded /o u/, 1 = spread /i e/)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Lips",
                  "type": "number"
                },
                "nasality": {
                  "default": 0.0,
                  "description": "Velar port opening (0 = oral, 1 = nasal /m n ŋ/)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Nasality",
                  "type": "number"
                },
                "tongue": {
                  "default": 0.5,
                  "description": "Constriction position (0 = throat, 1 = lips)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Tongue",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "vocal_tract",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Vocal Tract",
          "type": "object"
        },
        {
          "description": "FOF (fof) — CHANT-style granular formant voice / choir (FOF / formant wave functions)",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `fof-1`.",
              "pattern": "^fof-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "bandwidth": {
                  "default": 0.5,
                  "description": "Formant bandwidth: 0.5 = natural, <0.5 narrower/sharper, >0.5 wider",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Bandwidth",
                  "type": "number"
                },
                "breathiness": {
                  "default": 0.0,
                  "description": "Aspiration / breath noise amount",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Breathiness",
                  "type": "number"
                },
                "formant_shift": {
                  "default": 0.5,
                  "description": "Vocal-tract length: <0.5 longer/darker, >0.5 shorter/brighter",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Formant Shift",
                  "type": "number"
                },
                "level": {
                  "default": 0.800000011920929,
                  "description": "Output level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "skirt": {
                  "default": 0.30000001192092896,
                  "description": "Grain attack / formant skirt: 0 sharp & bright, 1 soft & dull",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Skirt",
                  "type": "number"
                },
                "unison_detune": {
                  "default": 15.0,
                  "description": "Pitch spread across unison voices — unit: ct",
                  "maximum": 50.0,
                  "minimum": 0.0,
                  "title": "Unison Detune",
                  "type": "number"
                },
                "unison_spread": {
                  "default": 0.699999988079071,
                  "description": "Stereo width of the choir (0 = mono, 1 = full)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Unison Spread",
                  "type": "number"
                },
                "unison_voices": {
                  "default": 0.0,
                  "description": "Choir size: 1 (solo) to 16 decorrelated voices",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Unison Voices",
                  "type": "number"
                },
                "vibrato_depth": {
                  "default": 0.0,
                  "description": "Vibrato depth in cents — unit: ct",
                  "maximum": 100.0,
                  "minimum": 0.0,
                  "title": "Vibrato Depth",
                  "type": "number"
                },
                "vibrato_rate": {
                  "default": 5.5,
                  "description": "Vibrato LFO rate — unit: Hz",
                  "maximum": 12.0,
                  "minimum": 0.0,
                  "title": "Vibrato Rate",
                  "type": "number"
                },
                "vowel": {
                  "default": 0.0,
                  "description": "Vowel morph (A → E → I → O → U)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Vowel",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "fof",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "FOF",
          "type": "object"
        },
        {
          "description": "Script (script) — Write your own control module in YAMS: one program with 4 CV inputs (in1..in4) and 4 CV outputs (out1..out4). Wire cables into in1..in4 and out of out1..out4; read/write them by name in the script.",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `scr-1`.",
              "pattern": "^scr-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {},
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "script",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Script",
          "type": "object"
        },
        {
          "description": "Audio Script (audio_script) — Audio-rate scripted DSP (one YAMS eval per sample): waveshaper, bitcrusher, ring-mod, custom IIR. Stereo in/out.",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `asc-1`.",
              "pattern": "^asc-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {},
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "audio_script",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Audio Script",
          "type": "object"
        },
        {
          "description": "SID Oscillator (sid_oscillator) — MOS 6581/8580 (SID) waveform generator: combinable waveforms from a 24-bit accumulator, raw frequency/pulse-width registers, MSB-based ring/hard-sync, and a per-frame waveform-mask sequence",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `sid-1`.",
              "pattern": "^sid-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "clock": {
                  "description": "Master clock standard for the pitch mapping",
                  "enum": [
                    "pal",
                    "ntsc"
                  ],
                  "title": "Clock",
                  "type": "string"
                },
                "dc_block": {
                  "default": 1.0,
                  "description": "One-pole DC blocker (~16 Hz, the C64 output coupling). Combined 6581 waveforms carry real DC that eats mix headroom",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "DC Block",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "freq_reg": {
                  "default": 0.0,
                  "description": "Raw 16-bit SID frequency register — used when Track Pitch is off (e.g. a ring/sync source held at a neighbour voice's pitch). Mod-matrix modulatable",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Freq Reg",
                  "type": "number"
                },
                "glide_time": {
                  "default": 0.0,
                  "description": "Per-oscillator portamento time (0 = follow the voice glide) — unit: s",
                  "maximum": 2.0,
                  "minimum": 0.0,
                  "title": "Glide",
                  "type": "number"
                },
                "hard_sync": {
                  "default": 0.0,
                  "description": "SYNC bit: reset the accumulator on the `sync` input's MSB 0→1 rising edge",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Hard Sync",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "level": {
                  "default": 1.0,
                  "description": "Output level",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Level",
                  "type": "number"
                },
                "model": {
                  "description": "Chip model: combined-waveform behaviour + DAC curve",
                  "enum": [
                    "6581",
                    "8580"
                  ],
                  "title": "Model",
                  "type": "string"
                },
                "noise": {
                  "default": 0.0,
                  "description": "Noise waveform bit (23-bit LFSR clocked by the accumulator)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Noise",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "noise_seed": {
                  "default": 8388607.0,
                  "description": "Non-zero 23-bit LFSR state loaded by reset and TEST",
                  "maximum": 8388607.0,
                  "minimum": 1.0,
                  "title": "Noise Seed",
                  "type": "number"
                },
                "pulse": {
                  "default": 0.0,
                  "description": "Pulse waveform bit (combinable, width from PW Reg)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Pulse",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "pw_reg": {
                  "default": 2048.0,
                  "description": "Raw 12-bit pulse-width register (2048 = square). Mod-matrix modulatable — PWM is a core SID idiom",
                  "maximum": 4095.0,
                  "minimum": 0.0,
                  "title": "PW Reg",
                  "type": "number"
                },
                "quality": {
                  "description": "Clock-domain conversion strategy (CPU vs fidelity)",
                  "enum": [
                    "fast",
                    "high"
                  ],
                  "title": "Quality",
                  "type": "string"
                },
                "ring_mod": {
                  "default": 0.0,
                  "description": "RING bit: the triangle folding direction XORs with the `ring` input's MSB (triangle only, like the chip)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Ring Mod",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "sawtooth": {
                  "default": 1.0,
                  "description": "Sawtooth waveform bit (combinable)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Sawtooth",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "seq_freq_mask": {
                  "default": 0.0,
                  "description": "Bit mask selecting sequence steps with an explicit frequency",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Freq Mask",
                  "type": "number"
                },
                "seq_len": {
                  "default": 0.0,
                  "description": "Active waveform-sequence steps (0 = off, the static waveform bits apply). Past the last step the sequence holds it, or repeats when Seq Loop is on",
                  "maximum": 16.0,
                  "minimum": 0.0,
                  "title": "Seq Length",
                  "type": "number"
                },
                "seq_loop": {
                  "default": 0.0,
                  "description": "Loop the waveform sequence for the whole note (off = hold the last step — drum-attack programs)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Seq Loop",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "seq_rate": {
                  "default": 1.0,
                  "description": "Driver frames (50/60 Hz) per waveform-sequence step",
                  "maximum": 16.0,
                  "minimum": 1.0,
                  "title": "Seq Rate",
                  "type": "number"
                },
                "seq_step_0": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 0",
                  "type": "number"
                },
                "seq_step_1": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 1",
                  "type": "number"
                },
                "seq_step_10": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 10",
                  "type": "number"
                },
                "seq_step_11": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 11",
                  "type": "number"
                },
                "seq_step_12": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 12",
                  "type": "number"
                },
                "seq_step_13": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 13",
                  "type": "number"
                },
                "seq_step_14": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 14",
                  "type": "number"
                },
                "seq_step_15": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 15",
                  "type": "number"
                },
                "seq_step_2": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 2",
                  "type": "number"
                },
                "seq_step_3": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 3",
                  "type": "number"
                },
                "seq_step_4": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 4",
                  "type": "number"
                },
                "seq_step_5": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 5",
                  "type": "number"
                },
                "seq_step_6": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 6",
                  "type": "number"
                },
                "seq_step_7": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 7",
                  "type": "number"
                },
                "seq_step_8": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 8",
                  "type": "number"
                },
                "seq_step_9": {
                  "default": 0.0,
                  "description": "Waveform mask for this step (bit 0 = triangle, 1 = sawtooth, 2 = pulse, 3 = noise)",
                  "maximum": 15.0,
                  "minimum": 0.0,
                  "title": "Seq Step 9",
                  "type": "number"
                },
                "seq_step_freq_0": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 0",
                  "type": "number"
                },
                "seq_step_freq_1": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 1",
                  "type": "number"
                },
                "seq_step_freq_10": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 10",
                  "type": "number"
                },
                "seq_step_freq_11": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 11",
                  "type": "number"
                },
                "seq_step_freq_12": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 12",
                  "type": "number"
                },
                "seq_step_freq_13": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 13",
                  "type": "number"
                },
                "seq_step_freq_14": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 14",
                  "type": "number"
                },
                "seq_step_freq_15": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 15",
                  "type": "number"
                },
                "seq_step_freq_2": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 2",
                  "type": "number"
                },
                "seq_step_freq_3": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 3",
                  "type": "number"
                },
                "seq_step_freq_4": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 4",
                  "type": "number"
                },
                "seq_step_freq_5": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 5",
                  "type": "number"
                },
                "seq_step_freq_6": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 6",
                  "type": "number"
                },
                "seq_step_freq_7": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 7",
                  "type": "number"
                },
                "seq_step_freq_8": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 8",
                  "type": "number"
                },
                "seq_step_freq_9": {
                  "default": 0.0,
                  "description": "Raw SID frequency register for this sequence step",
                  "maximum": 65535.0,
                  "minimum": 0.0,
                  "title": "Seq Step Freq 9",
                  "type": "number"
                },
                "test": {
                  "default": 0.0,
                  "description": "TEST bit: zeroes and holds the accumulator (hard restart) and reloads the noise LFSR",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Test",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "track_pitch": {
                  "default": 1.0,
                  "description": "Derive Freq Reg from the played note (on) or hold the authored Freq Reg (off — ring/sync-source tuning)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Track Pitch",
                  "type": [
                    "number",
                    "boolean"
                  ]
                },
                "triangle": {
                  "default": 0.0,
                  "description": "Triangle waveform bit (combinable)",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Triangle",
                  "type": [
                    "number",
                    "boolean"
                  ]
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "sid_oscillator",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "SID Oscillator",
          "type": "object"
        },
        {
          "description": "Spatial Panner (spatial_panner) — Per-voice binaural positioning: ISM early reflections + ITD/ILD direct path",
          "properties": {
            "description": {
              "description": "Free-text per-instance note (distinct from the module *type* description above). Omitted when empty.",
              "type": "string"
            },
            "id": {
              "description": "Module instance ID, e.g. `spp-1`.",
              "pattern": "^spp-\\d+$",
              "type": "string"
            },
            "parameters": {
              "additionalProperties": false,
              "default": {},
              "description": "Parameter values for this module.",
              "properties": {
                "absorption": {
                  "default": 0.4000000059604645,
                  "description": "Wall absorption (0 = reflective, 1 = dead) — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Absorption",
                  "type": "number"
                },
                "air_absorption": {
                  "default": 0.10000000149011612,
                  "description": "High-frequency damping over distance — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Air Absorption",
                  "type": "number"
                },
                "diffusion": {
                  "default": 0.30000001192092896,
                  "description": "Scattering of the reflections (0 = specular, 1 = diffuse) — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Diffusion",
                  "type": "number"
                },
                "direct_level": {
                  "default": 1.0,
                  "description": "Direct (binaural) path level — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Direct Level",
                  "type": "number"
                },
                "distance": {
                  "default": 1.0,
                  "description": "Radial distance of the source (1 = full X/Y/Z offset, 0 = at the listener) — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "Distance",
                  "type": "number"
                },
                "er_level": {
                  "default": 0.4000000059604645,
                  "description": "Early-reflection (room) level — unit: %",
                  "maximum": 1.0,
                  "minimum": 0.0,
                  "title": "ER Level",
                  "type": "number"
                },
                "x": {
                  "default": 0.0,
                  "description": "Left/right source offset (-1 = left, +1 = right)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "X",
                  "type": "number"
                },
                "y": {
                  "default": 0.4000000059604645,
                  "description": "Front/back source offset (-1 = behind, +1 = in front)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Y",
                  "type": "number"
                },
                "z": {
                  "default": 0.0,
                  "description": "Down/up source offset (-1 = below, +1 = above)",
                  "maximum": 1.0,
                  "minimum": -1.0,
                  "title": "Z",
                  "type": "number"
                }
              },
              "type": "object"
            },
            "position": {
              "$ref": "#/$defs/Position"
            },
            "scripts": {
              "additionalProperties": {
                "type": "string"
              },
              "default": {},
              "description": "Per-slot YAMS control scripts (Step 2), keyed by 1-based slot number as a string (e.g. `\"1\"`). Held separate from `parameters` because a script is canonical source text compiled on load, not a descriptor-driven value. Omitted when empty.",
              "type": "object"
            },
            "type": {
              "const": "spatial_panner",
              "description": "Module type discriminator."
            }
          },
          "required": [
            "id",
            "type",
            "position"
          ],
          "title": "Spatial Panner",
          "type": "object"
        }
      ]
    },
    "ModuleType": {
      "description": "Strongly-typed module type identifier.\n\nVariant declaration order is load-bearing: `Ord` is derived, and\n`ModuleGraph` orders nodes by `(ModuleType, instance)` to keep offline\nrenders bit-exact across calls. Reordering or inserting variants\nchanges the processing order for any voice graph that mixes the\naffected types, which silently changes audio output for existing\npatches. Append new variants at the end.",
      "oneOf": [
        {
          "enum": [
            "oscillator",
            "math_oscillator",
            "sub_oscillator",
            "noise",
            "filter",
            "envelope",
            "lfo",
            "amplifier",
            "mixer",
            "stereo_output",
            "delay",
            "reverb",
            "distortion",
            "chorus",
            "phaser",
            "flanger",
            "compressor",
            "eq",
            "waveshaper",
            "oscilloscope",
            "level_meter",
            "spectrum_analyzer",
            "mod_matrix",
            "ring_mod",
            "envelope_follower",
            "wavetable_osc",
            "mseg",
            "additive_osc",
            "bbd_delay",
            "mid_side",
            "limiter",
            "euclidean",
            "turing_machine",
            "random_gates",
            "keyboard_panner",
            "body_resonance",
            "mechanical_noise",
            "granular_osc",
            "convolver",
            "phase_vocoder",
            "kinetic_modulator",
            "signal_monitor",
            "frequency_shifter",
            "vector_mixer",
            "la_synth",
            "pitch_tracker",
            "ensemble_chorus",
            "shimmer_reverb",
            "granular_fx",
            "spectral_blur",
            "modal_resonator",
            "reverse_gate_reverb",
            "fractal_osc",
            "sampler",
            "audio_input",
            "ladder_filter",
            "drift_generator",
            "chaotic_osc",
            "formant_filter",
            "fooglers",
            "beat_detector",
            "pad_synth",
            "am_formant",
            "tilt_eq",
            "univibe",
            "crossover_splitter",
            "vocoder",
            "voice_synth",
            "vocal_tract",
            "fof",
            "script"
          ],
          "type": "string"
        },
        {
          "const": "transient_shaper",
          "description": "Differential-envelope transient designer: independent attack/sustain\ngain shaping without altering the underlying amplitude.",
          "type": "string"
        },
        {
          "const": "audio_script",
          "description": "Per-voice audio-rate scripted DSP (YAMS, one eval per sample): waveshaper,\nbitcrusher, ring-mod, custom IIR/filter. Audio in/out ports.",
          "type": "string"
        },
        {
          "const": "sid_oscillator",
          "description": "MOS 6581/8580 (SID) waveform generator: combinable waveforms, noise\nLFSR, ring/hard-sync, TEST, per-model DAC.",
          "type": "string"
        },
        {
          "const": "spatial_panner",
          "description": "Per-voice binaural spatial panner: ISM early reflections + ITD/ILD\ndirect path positioning a mono voice in a fixed virtual room.",
          "type": "string"
        }
      ]
    },
    "NodePosition": {
      "description": "A node's canvas position in the Note Grid editor (world coordinates).\nLayout metadata only — evaluation ignores it; a node without one gets\nauto-layout.",
      "properties": {
        "x": {
          "format": "float",
          "type": "number"
        },
        "y": {
          "format": "float",
          "type": "number"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "NormalizedValue": {
      "description": "A value normalized to the range [0.0, 1.0].\n\nUsed for:\n- Knob positions\n- Envelope levels\n- Mix amounts\n- Pulse width\n- Any parameter where 0 = min and 1 = max",
      "format": "float",
      "type": "number"
    },
    "Note": {
      "description": "A note in a pattern.",
      "examples": [
        {
          "duration": 480,
          "id": 0,
          "pitch": 60,
          "start": 0,
          "velocity": 0.8
        }
      ],
      "properties": {
        "duration": {
          "anyOf": [
            {
              "$ref": "#/$defs/Duration"
            },
            {
              "type": "null"
            }
          ],
          "description": "Duration in ticks."
        },
        "expression": {
          "anyOf": [
            {
              "$ref": "#/$defs/NoteExpression"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Per-note expression block (vibrato + note-shape scalars). Additive;\ndefaults to `None`."
        },
        "glide": {
          "anyOf": [
            {
              "$ref": "#/$defs/Glide"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Per-note glide (portamento/glissando). Additive; defaults to `None`."
        },
        "id": {
          "$ref": "#/$defs/NoteId",
          "description": "Unique ID for this note (for editing/selection)."
        },
        "lane": {
          "$ref": "#/$defs/NoteLane",
          "default": 0,
          "description": "Voice lane this note occupies in the tracker grid. A pure editor/layout\nconcept — ignored by the piano roll and at playback. Additive; defaults\nto lane 0, so pre-lane projects load with every note in the first column."
        },
        "legato": {
          "default": false,
          "description": "Tie / legato intent: when set, this note is a legato continuation of its\npredecessor — it glides onto the active voice without re-gating (taxonomy\nprimitive 2). The flag sits on the *successor* (the later note of the\ntie), matching the engine's coalesce path. Additive; defaults to `false`.",
          "type": "boolean"
        },
        "note_graph": {
          "anyOf": [
            {
              "$ref": "#/$defs/NoteGraphId"
            },
            {
              "type": "null"
            }
          ],
          "description": "Per-note Note Grid graph (the note scope, plan §2.1) — the generalization\nof `ornament`: this note's material runs through the referenced pooled\ngraph during source collection, before the pattern-scope graph. Additive;\ndefaults to `None` (no note-scope processing)."
        },
        "ornament": {
          "anyOf": [
            {
              "$ref": "#/$defs/Ornament"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Per-note timed-repeat ornament (flam/drag/ruff/roll/grace). Expanded at\nplayback time (Model B), never baked into storage. Additive; defaults\nto `None`."
        },
        "pitch": {
          "$ref": "#/$defs/Pitch",
          "description": "Pitch."
        },
        "start": {
          "$ref": "#/$defs/PatternTick",
          "description": "Start position within the pattern."
        },
        "track": {
          "anyOf": [
            {
              "$ref": "#/$defs/TrackId"
            },
            {
              "type": "null"
            }
          ],
          "description": "Track/channel for mono-per-track behavior."
        },
        "velocity": {
          "$ref": "#/$defs/Velocity",
          "description": "Velocity/attack strength."
        }
      },
      "required": [
        "id",
        "start",
        "pitch",
        "velocity"
      ],
      "type": "object"
    },
    "NoteConnection": {
      "description": "A directed connection between two nodes' ports of a given signal type.\n\nv1 stores only `NoteStream` links (the linear spine). The type is carried so\nthe DAG escalation can add `Value`/`Gate` edges without a format change.",
      "properties": {
        "from": {
          "$ref": "#/$defs/NoteModuleId",
          "description": "Source node (output side)."
        },
        "port": {
          "$ref": "#/$defs/NotePortType",
          "description": "Signal type carried by this edge."
        },
        "to": {
          "$ref": "#/$defs/NoteModuleId",
          "description": "Destination node (input side)."
        },
        "to_input": {
          "default": 0,
          "description": "For `Value`/`Gate` edges, which of the target's value input ports this\nfeeds (`0..value_input_count`). Ignored for `NoteStream` edges.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "from",
        "to",
        "port"
      ],
      "type": "object"
    },
    "NoteDelay": {
      "description": "A decaying echo (plan §5.A): at tick `t` it\nkeeps the dry stream and appends copies of the onsets that entered this node\nat ticks `t − k·delay_ticks` (`k = 1..=repeats`), each scaled by `feedback^k`\nin velocity. Pure look-back — no queue, no cross-tick state. On the spine\nhead the scan reads the seeded source directly; downstream of a transform it\nre-runs the upstream prefix at each probed tick via\n`NoteGraph::eval_prefix_at_tick`. RT-safe: bounded by `repeats`,\n[`MAX_NOTE_DELAY_TICKS`], and the buffer cap.",
      "properties": {
        "delay_ticks": {
          "$ref": "#/$defs/Duration",
          "default": 480,
          "description": "Ticks between successive echoes."
        },
        "feedback": {
          "$ref": "#/$defs/NormalizedValue",
          "default": 0.6000000238418579,
          "description": "Per-echo velocity multiplier (`0.0` = instant silence, `1.0` = no decay)."
        },
        "repeats": {
          "default": 3,
          "description": "Echoes after the dry note (clamped to [`MAX_DELAY_REPEATS`]).",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "NoteEnvelope": {
      "description": "An attack/decay envelope `Value` source. Level rises to `peak` over `attack`,\nfalls to zero over `decay`, then holds at zero, retriggered per `trigger`\n(plan §5.C).",
      "properties": {
        "attack": {
          "$ref": "#/$defs/Duration",
          "default": 0,
          "description": "Ticks from onset to `peak`."
        },
        "decay": {
          "$ref": "#/$defs/Duration",
          "default": 480,
          "description": "Ticks from `peak` back to zero."
        },
        "peak": {
          "$ref": "#/$defs/NormalizedValue",
          "default": 1.0,
          "description": "Peak level (`0.0..1.0`)."
        },
        "trigger": {
          "$ref": "#/$defs/EnvelopeTrigger",
          "default": "SourceOnset",
          "description": "What restarts the envelope (source onsets vs. transformed-stream onsets)."
        }
      },
      "type": "object"
    },
    "NoteExpression": {
      "description": "Per-note expression block — *note expression in miniature*.\n\nTwo taxonomy groups, both `Copy`/alloc-free:\n- **primitive 1** — a parametric modulator ([`Vibrato`]). The block is named\n  against the MPE / MIDI-2.0 minimal dimension set (*bend, pressure,\n  timbre/slide, velocity, release-velocity*) so the Phase E hand-drawn curve\n  editor extends this block rather than replacing it; only vibrato (a\n  bend-dimension modulator) is wired in Phase C.\n- **primitive 3** — note-shape scalars that shape a single note without a\n  curve: `accent` (velocity ×), `gate` (% of duration), `ghost`, `probability`.",
      "properties": {
        "accent": {
          "default": null,
          "description": "Accent: velocity multiplier (1.0 = unchanged, >1 louder). Applied before\nthe note is triggered. Dimensionless ratio.",
          "format": "float",
          "type": [
            "number",
            "null"
          ]
        },
        "gate": {
          "anyOf": [
            {
              "$ref": "#/$defs/NormalizedValue"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Gate length as a fraction of the note's duration (staccato/tenuto).\n`None` = full duration."
        },
        "ghost": {
          "default": false,
          "description": "Ghost note: forced low velocity.",
          "type": "boolean"
        },
        "probability": {
          "anyOf": [
            {
              "$ref": "#/$defs/NormalizedValue"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Trigger probability (0.0–1.0). Resolved sequencer-side at emission\n(Phase C2), never with RNG on the audio thread. `None` = always plays.\nThe roll keys on note **identity** + absolute tick + a loop nonce\n(`note_passes_probability`), so it **re-rolls each loop pass** and needs\nno note-graph. This is the authored per-note layer and is *not* made\nredundant by the graph's `ProbabilityGate`, which holds one\nposition-keyed probability per scope with no loop re-roll — see that\ntype's doc for why the two coexist."
        },
        "vibrato": {
          "anyOf": [
            {
              "$ref": "#/$defs/Vibrato"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Per-note vibrato (primitive 1)."
        }
      },
      "type": "object"
    },
    "NoteGraph": {
      "description": "Static note-graph configuration — a pooled project asset.",
      "properties": {
        "color": {
          "anyOf": [
            {
              "$ref": "#/$defs/TrackColor"
            },
            {
              "type": "null"
            }
          ],
          "description": "Sequencer-side color (RGB), never egui's `Color32` — this crate has no\negui dependency."
        },
        "connections": {
          "default": [],
          "description": "Connections, kept as a sorted `Vec` for deterministic serialization and\niteration. v1: `NoteStream` axis validated linear; `Value`/`Gate` edges\npermitted once modulators exist.",
          "items": {
            "$ref": "#/$defs/NoteConnection"
          },
          "type": "array"
        },
        "description": {
          "default": "",
          "description": "Free-text description.",
          "type": "string"
        },
        "id": {
          "$ref": "#/$defs/NoteGraphId",
          "description": "Pool-unique id."
        },
        "name": {
          "description": "User-facing name, shown in the pool view.",
          "type": "string"
        },
        "node_descriptions": {
          "additionalProperties": false,
          "description": "Pedagogical/user intent per node, kept separate from DSP configs.",
          "patternProperties": {
            "^\\d+$": {
              "type": "string"
            }
          },
          "type": "object"
        },
        "node_positions": {
          "additionalProperties": false,
          "description": "Editor canvas positions per node (layout metadata; nodes without one\nare auto-laid-out). Persisted so an arranged canvas survives reload.",
          "patternProperties": {
            "^\\d+$": {
              "$ref": "#/$defs/NodePosition"
            }
          },
          "type": "object"
        },
        "nodes": {
          "additionalProperties": false,
          "default": {},
          "description": "Nodes keyed by graph-local id.",
          "patternProperties": {
            "^\\d+$": {
              "$ref": "#/$defs/NoteModuleConfig"
            }
          },
          "type": "object"
        }
      },
      "required": [
        "id",
        "name"
      ],
      "type": "object"
    },
    "NoteGraphId": {
      "description": "Unique identifier for a Note Grid graph in the project pool.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "NoteId": {
      "description": "Unique identifier for a note within a pattern.\nUsed for selection, undo/redo, and editing operations.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "NoteLane": {
      "description": "Voice/polyphony lane a note occupies in the tracker grid (0-255).\n\nPurely an editor/layout concept: it groups simultaneous notes into stable\ntracker columns and is **not** consulted at playback. Defaults to lane 0, so\npiano-roll-created notes and pre-lane projects all land in the first column.\nDistinct from [`TrackId`] (mono-per-track routing) and from engine voice\nallocation — this only describes a note's column in the tracker view.",
      "format": "uint8",
      "maximum": 255,
      "minimum": 0,
      "type": "integer"
    },
    "NoteLfo": {
      "description": "A tempo-synced control LFO producing a unipolar `Value` (`0.0..1.0`) from\npure tick arithmetic — phase = `f(tick)`, no cross-tick state (plan §5.C).\nFeeds a downstream node's `Value` input (e.g. a script's `in1`, or a\n[`ProbabilityGate`] threshold).",
      "properties": {
        "depth": {
          "$ref": "#/$defs/NormalizedValue",
          "default": 1.0,
          "description": "Output depth — scales the `0.0..1.0` level (1.0 = full range)."
        },
        "period": {
          "$ref": "#/$defs/Duration",
          "default": 960,
          "description": "Ticks per cycle (0 = frozen at the phase offset)."
        },
        "phase": {
          "$ref": "#/$defs/NormalizedValue",
          "default": 0.0,
          "description": "Phase offset, `0.0..1.0` of a cycle."
        },
        "shape": {
          "$ref": "#/$defs/LfoShape",
          "default": "Sine",
          "description": "Waveform shape."
        }
      },
      "type": "object"
    },
    "NoteModuleConfig": {
      "description": "Static configuration of one graph node — the pattern-rack DSP or a native\nNote Grid module.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "A stream transform/generator reusing the pattern rack's DSP.",
          "properties": {
            "Processor": {
              "$ref": "#/$defs/NoteProcessor"
            }
          },
          "required": [
            "Processor"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A Euclidean rhythm generator (source-independent).",
          "properties": {
            "Euclidean": {
              "$ref": "#/$defs/EuclideanGenerator"
            }
          },
          "required": [
            "Euclidean"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A seeded pass/block probability filter.",
          "properties": {
            "ProbabilityGate": {
              "$ref": "#/$defs/ProbabilityGate"
            }
          },
          "required": [
            "ProbabilityGate"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A tempo-synced control LFO (`Value` source).",
          "properties": {
            "NoteLfo": {
              "$ref": "#/$defs/NoteLfo"
            }
          },
          "required": [
            "NoteLfo"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A step-sequenced control LFO (`Value` source).",
          "properties": {
            "StepLfo": {
              "$ref": "#/$defs/StepLfo"
            }
          },
          "required": [
            "StepLfo"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "An attack/decay envelope retriggered by source onsets (`Value` source).",
          "properties": {
            "NoteEnvelope": {
              "$ref": "#/$defs/NoteEnvelope"
            }
          },
          "required": [
            "NoteEnvelope"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A per-event YAMS `note_event` script (1-in/1-out stream transform).",
          "properties": {
            "NoteScriptTransform": {
              "$ref": "#/$defs/NoteScriptTransform"
            }
          },
          "required": [
            "NoteScriptTransform"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A decaying echo (timing node with bounded look-back, plan §5.A).",
          "properties": {
            "NoteDelay": {
              "$ref": "#/$defs/NoteDelay"
            }
          },
          "required": [
            "NoteDelay"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "A ratchet / roll: subdivides each note into fast retriggers (timing node).",
          "properties": {
            "Ratchet": {
              "$ref": "#/$defs/Ratchet"
            }
          },
          "required": [
            "Ratchet"
          ],
          "type": "object"
        }
      ]
    },
    "NoteModuleId": {
      "description": "Unique identifier for a module (node) within a single [`NoteGraphId`] graph.\n\nOnly unique within its owning graph — different graphs reuse the same small\nid space.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "NotePortType": {
      "description": "The three signal types that flow through Note Grid connections, all\nevaluated at tick rate. `NoteStream` is the linear spine; `Value`/`Gate`\nare side-input modulation edges from a source node to a downstream node's\nvalue input (they do not branch the stream, plan §3).",
      "oneOf": [
        {
          "const": "NoteStream",
          "description": "The per-tick set of expanded notes (the spine).",
          "type": "string"
        },
        {
          "const": "Value",
          "description": "A continuous control scalar, recomputed per tick.",
          "type": "string"
        },
        {
          "const": "Gate",
          "description": "A trigger high on specific ticks.",
          "type": "string"
        }
      ]
    },
    "NoteProcessor": {
      "description": "One processor in a pattern's rack — pure *config* (Model B), never baked\noutput. Serialized with the pattern, so it round-trips save/load with the\nsame path as notes and automation.\n\n`Copy` (every variant's config is `Copy`): the Note Grid collects a spine's\nupstream processors into a fixed stack array to reuse the arp / strummed-chord\nheld-pitch view (`expand_held_pitch`) — no allocation on the audio thread.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "Snap pitches to a scale (chain stage 0).",
          "properties": {
            "ScaleQuantize": {
              "$ref": "#/$defs/ScaleQuantize"
            }
          },
          "required": [
            "ScaleQuantize"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Expand each source note into a chord (chain stage 1).",
          "properties": {
            "Chord": {
              "$ref": "#/$defs/Chord"
            }
          },
          "required": [
            "Chord"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Arpeggiate the held chord (chain stage 2).",
          "properties": {
            "Arpeggiator": {
              "$ref": "#/$defs/Arpeggiator"
            }
          },
          "required": [
            "Arpeggiator"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Seeded random perturbation of the final stream (chain stage 4).",
          "properties": {
            "Humanize": {
              "$ref": "#/$defs/Humanize"
            }
          },
          "required": [
            "Humanize"
          ],
          "type": "object"
        }
      ]
    },
    "NoteScriptTransform": {
      "description": "A per-event YAMS `note_event` script transforming the stream 1:1 (plan §6.1).\n\nHolds the canonical `source` (serialized) and a compiled `BoundScript`\n(`#[serde(skip)]` — rebuilt from `source` on load / edit by a non-RT sync\nstep, never on the audio thread). With no compiled program the node is\npass-through. Pure by construction: the VM is deterministic and per-event\nstate resets.",
      "properties": {
        "source": {
          "description": "The YAMS `note_event` source text.",
          "type": "string"
        }
      },
      "required": [
        "source"
      ],
      "type": "object"
    },
    "Ornament": {
      "additionalProperties": false,
      "description": "Per-note timed-repeat ornament — taxonomy **primitive 4** (a generator).\n\nOne generator covers flam / drag / ruff / roll and the grace note: emit\n`count` hits of the note offset in time, with a spacing and velocity curve.\nExactly one hit is the *main* hit — it carries the source pitch, duration,\nand per-note expression/glide; the rest are short *grace* hits at\n`pitch_offset` semitones (0 for same-pitch drum figures, non-zero for an\nacciaccatura). `Copy`/alloc-free so it stays RT-cheap on the audio thread.\n\nPreset mappings (drum-rudiment convention, by total `count`): flam = 2,\ndrag = 3, ruff = 4, roll = N; acciaccatura = 2 with a `pitch_offset`. A\n`count` ≤ 1 is a no-op (the note plays unchanged). Trill / mordent / turn\nare *not* here — those alternate pitches and are arpeggiator presets (NP2).",
      "properties": {
        "count": {
          "default": 2,
          "description": "Total number of struck hits (including the main hit). ≤ 1 is a no-op;\nbounded at expansion by the engine's hit cap.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "dynamics": {
          "$ref": "#/$defs/OrnamentDynamics",
          "default": "Flat",
          "description": "How hit velocity evolves across the figure."
        },
        "grace_gate": {
          "$ref": "#/$defs/NormalizedValue",
          "default": 0.5,
          "description": "Grace-hit duration as a fraction of its gap to the next hit (short =\ncrushed). The main hit keeps the source note's duration."
        },
        "pitch_offset": {
          "$ref": "#/$defs/Semitones",
          "default": 0.0,
          "description": "Semitone offset of the grace hits relative to the main pitch (0 = a\nsame-pitch roll/flam; e.g. `+2` = an acciaccatura a tone above)."
        },
        "placement": {
          "$ref": "#/$defs/OrnamentPlacement",
          "default": "LeadIn",
          "description": "Where the figure sits relative to the beat."
        },
        "spacing": {
          "$ref": "#/$defs/Duration",
          "default": 120,
          "description": "Base tick gap between adjacent hits (the curve warps it)."
        },
        "spacing_curve": {
          "$ref": "#/$defs/OrnamentSpacing",
          "default": "Even",
          "description": "How the gaps evolve across the figure."
        }
      },
      "type": "object"
    },
    "OrnamentDynamics": {
      "description": "How hit velocity evolves across an ornament's figure.\n\nA lead-in crescendo is the canonical flam/ruff shape (quiet graces swelling\ninto the main hit on the beat); decrescendo is the buzz that dies away.",
      "oneOf": [
        {
          "const": "Flat",
          "description": "Every hit at the source velocity.",
          "type": "string"
        },
        {
          "const": "Crescendo",
          "description": "Velocity rises from a floor to the source velocity across the figure.",
          "type": "string"
        },
        {
          "const": "Decrescendo",
          "description": "Velocity falls from the source velocity to a floor across the figure.",
          "type": "string"
        }
      ]
    },
    "OrnamentPlacement": {
      "description": "Where an ornament's figure sits relative to the note's start tick.",
      "oneOf": [
        {
          "const": "LeadIn",
          "description": "Grace hits precede the beat; the *main* hit (the source pitch) lands\nexactly on `note.start`. Flam / drag / ruff / acciaccatura.",
          "type": "string"
        },
        {
          "const": "OnBeat",
          "description": "The main hit is on the beat; grace hits follow it (nachschlag / a roll\nthat decays after the downbeat).",
          "type": "string"
        }
      ]
    },
    "OrnamentSpacing": {
      "description": "How the gaps between an ornament's hits evolve across the figure.\n\nDrum/keyboard ornaments are rarely metronomic — a press roll accelerates,\na drag opens up. The curve warps the *onset* spacing; the hit count and the\ntotal span stay fixed (the last hit always lands on the figure's anchor).",
      "oneOf": [
        {
          "const": "Even",
          "description": "Even gaps (a metronomic roll).",
          "type": "string"
        },
        {
          "const": "Accelerate",
          "description": "Gaps shrink across the figure (the ornament speeds up).",
          "type": "string"
        },
        {
          "const": "Decelerate",
          "description": "Gaps grow across the figure (the ornament opens up).",
          "type": "string"
        }
      ]
    },
    "ParamId": {
      "description": "Interned-by-`Arc` parameter id for [`AutomationTarget::Module`].\n\nWraps `Arc<str>` so cloning the target on the audio thread (the per-tick\nautomation collection / event emission in `sequencer_engine`) is an atomic\nrefcount bump — RT-safe (no heap allocation, no lock), unlike the previous\n`String` which allocated on every clone.\n\nChosen over a global intern pool + `Copy` handle (the roadmap's original\nsketch): the engine dispatch matches the param id against each descriptor's\n`type_id` **string**, so an intern handle would force a lock-taking\n`as_str()` on the audio thread — trading an allocation for a lock. `Arc<str>`\nkeeps the dispatch's lock-free `&str` compare while making the clone cheap.\nSerializes transparently as the id string, so existing projects load unchanged.\n\n**Drop caveat (tracked follow-up):** dropping a clone on the audio thread is a\nrefcount *decrement*; only if the source lane was removed mid-playback (so the\nengine's cached clone in `last_automation_values` / the event buffer is the\nlast ref) does the next `clear()` free on the audio thread. This is a strict\nimprovement over the prior `String` (which freed on *every* drop) but not yet\nfully eliminated — the robust fix is to route cleared targets through the\nengine's existing off-thread drop channel (`return_producer`), deferred here.",
      "type": "string"
    },
    "Patch": {
      "description": "A complete synthesizer patch.",
      "properties": {
        "author": {
          "anyOf": [
            {
              "$ref": "#/$defs/Author"
            },
            {
              "type": "null"
            }
          ],
          "description": "Author information."
        },
        "color": {
          "description": "Optional accent color as a hex string (e.g. \"#FF8800FF\"), distinct from\nthe per-instance `InstrumentState.color`. Travels with the patch so a\nshared/exported patch carries its own suggested tint. Set via MCP\n(`set_patch_color`); `None` when unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "connections": {
          "description": "Connections between modules.",
          "items": {
            "$ref": "#/$defs/ConnectionState"
          },
          "type": "array"
        },
        "description": {
          "description": "Description of the patch.",
          "type": [
            "string",
            "null"
          ]
        },
        "groups": {
          "description": "Module groups (UI-level metadata).",
          "items": {
            "$ref": "#/$defs/ModuleGroupState"
          },
          "type": "array"
        },
        "modules": {
          "description": "Module configurations.",
          "items": {
            "$ref": "#/$defs/ModuleState"
          },
          "type": "array"
        },
        "name": {
          "description": "Patch name.",
          "type": "string"
        },
        "notes": {
          "description": "Detailed explanation of how the patch works.",
          "type": [
            "string",
            "null"
          ]
        },
        "settings": {
          "$ref": "#/$defs/PatchSettings",
          "default": {
            "bpm": 120.0,
            "glide_time": 0.0,
            "master_volume": 0.800000011920929,
            "octave_offset": 0
          },
          "description": "Global settings."
        },
        "tags": {
          "description": "Tags for categorization.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "version": {
          "default": "1.0",
          "description": "Patch version.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "modules",
        "connections"
      ],
      "type": "object"
    },
    "PatchSettings": {
      "description": "Global patch settings.",
      "properties": {
        "bpm": {
          "description": "Tempo in BPM.",
          "format": "float",
          "type": [
            "number",
            "null"
          ]
        },
        "canvas_size": {
          "anyOf": [
            {
              "$ref": "#/$defs/CanvasSize"
            },
            {
              "type": "null"
            }
          ],
          "description": "Canvas size hint for the rack view.\nUsed to restore the scroll area size when loading a patch."
        },
        "effect_chain_order": {
          "description": "Effect chain processing order (module IDs as strings, in chain order).\n\nThe engine restores the effect chain to match this order on load.\nAn empty list means the patch has no effect-chain modules.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "glide_time": {
          "default": 0.0,
          "description": "Glide/portamento time in seconds (0.0 = off).",
          "format": "float",
          "type": "number"
        },
        "master_volume": {
          "default": 0.800000011920929,
          "description": "Master volume (0.0 - 1.0).",
          "format": "float",
          "type": "number"
        },
        "octave_offset": {
          "default": 0,
          "description": "Octave offset for keyboard.",
          "format": "int32",
          "type": "integer"
        }
      },
      "type": "object"
    },
    "Pattern": {
      "description": "A pattern containing notes and automation.",
      "examples": [
        {
          "automation": [],
          "id": 0,
          "length": 3840,
          "name": "Verse",
          "next_note_id": 2,
          "notes": [
            {
              "duration": 960,
              "id": 0,
              "pitch": 60,
              "start": 0,
              "velocity": 0.8
            },
            {
              "duration": 960,
              "id": 1,
              "pitch": 64,
              "start": 960,
              "velocity": 0.8
            }
          ]
        }
      ],
      "properties": {
        "automation": {
          "description": "Automation lanes.",
          "items": {
            "$ref": "#/$defs/AutomationLane"
          },
          "type": "array"
        },
        "description": {
          "default": "",
          "description": "Free-text description capturing intent (e.g. \"chorus drop, half-time\nfeel\"). Empty by default; readable/writable via MCP and GUI.",
          "type": "string"
        },
        "id": {
          "$ref": "#/$defs/PatternId",
          "description": "Unique identifier."
        },
        "length": {
          "$ref": "#/$defs/Duration",
          "description": "Length in ticks."
        },
        "name": {
          "description": "Pattern name.",
          "type": "string"
        },
        "next_note_id": {
          "description": "Next note ID counter.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "note_graph": {
          "anyOf": [
            {
              "$ref": "#/$defs/NoteGraphId"
            },
            {
              "type": "null"
            }
          ],
          "description": "Optional pooled Note Grid graph this pattern expands through. When set,\nit takes precedence over the linear rack. A dangling id (graph removed\nfrom the pool) is treated as pass-through at expansion time."
        },
        "notes": {
          "description": "All notes, sorted by start tick.",
          "items": {
            "$ref": "#/$defs/Note"
          },
          "type": "array"
        },
        "processors": {
          "default": [],
          "description": "Legacy note-processor rack (Model B generative articulation), in\nexecution order. Retired in favour of the Note Grid: there is no product\nauthoring path anymore, and a loaded rack migrates to a pooled graph\n([`crate::Song::migrate_processor_racks_to_graphs`]). Kept `#[serde(default)]`\nso pre-migration projects still deserialize; the expansion + freeze\naccessors live in [`crate::note_processor`].",
          "items": {
            "$ref": "#/$defs/NoteProcessor"
          },
          "type": "array"
        }
      },
      "required": [
        "id",
        "name",
        "length",
        "notes",
        "automation",
        "next_note_id"
      ],
      "type": "object"
    },
    "PatternId": {
      "description": "Unique identifier for a pattern.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "PatternPlacement": {
      "description": "A pattern placement in the arrangement.",
      "properties": {
        "gain": {
          "$ref": "#/$defs/Gain",
          "description": "Volume scaling (1.0 = normal)."
        },
        "length_override": {
          "anyOf": [
            {
              "$ref": "#/$defs/Duration"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional per-placement length override. When `None`, the placement\noccupies exactly `pattern.length` ticks. When `Some(d)`, the\nplacement extends/clips to that length; [`PlacementLoopMode`] decides\nwhether playback repeats or stops at the source-pattern boundary."
        },
        "loop_mode": {
          "$ref": "#/$defs/PlacementLoopMode",
          "default": "repeat",
          "description": "Whether a placement longer than its source plays once or repeats."
        },
        "pattern_id": {
          "$ref": "#/$defs/PatternId",
          "description": "Which pattern to play."
        },
        "start": {
          "$ref": "#/$defs/Tick",
          "description": "Start position in the song."
        },
        "track_id": {
          "$ref": "#/$defs/TrackId",
          "description": "Which track this is on."
        },
        "transpose": {
          "$ref": "#/$defs/Semitones",
          "description": "Transposition in semitones."
        }
      },
      "required": [
        "pattern_id",
        "track_id",
        "start",
        "transpose",
        "gain"
      ],
      "type": "object"
    },
    "PatternTick": {
      "description": "Relative position within a pattern (0 = pattern start).",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "Pitch": {
      "description": "MIDI-compatible pitch (0-127).",
      "format": "uint8",
      "maximum": 127,
      "minimum": 0,
      "type": "integer"
    },
    "PitchClass": {
      "description": "A pitch class (0–11, C = 0). Constructor wraps modulo 12.",
      "format": "uint8",
      "maximum": 11,
      "minimum": 0,
      "type": "integer"
    },
    "PlacementLoopMode": {
      "description": "Playback behavior when a placement outlasts its source pattern.",
      "oneOf": [
        {
          "const": "clip",
          "description": "Play the source pattern once, leaving any remaining placement time silent.",
          "type": "string"
        },
        {
          "const": "repeat",
          "description": "Repeat the source pattern until the placement ends.",
          "type": "string"
        }
      ]
    },
    "Position": {
      "description": "2D position in the rack view, serialized as `{\"x\": ..., \"y\": ...}`.",
      "properties": {
        "x": {
          "format": "float",
          "type": "number"
        },
        "y": {
          "format": "float",
          "type": "number"
        }
      },
      "required": [
        "x",
        "y"
      ],
      "type": "object"
    },
    "ProbabilityGate": {
      "description": "A pass/block stream filter: each note survives with probability\n`probability`, decided by a seeded hash (reproducible, host-decorrelated).\nIt rolls its **own** draw rather than writing `NoteExpression::probability`,\nwhich the engine resolves on *source* notes only — a generated note's field\nwould never be re-gated (plan §5.D).\n\n**Not redundant with [`Note::probability`](crate::Note) — they are different\nlayers, do not fold one into the other:**\n- *This gate* holds **one** probability for its whole scope (optionally\n  modulated per-tick by a `threshold_mod` Value input); it cannot carry a\n  distinct value per note. Its roll keys on *position* (`PatternTick ^ pitch\n  ^ slot`, see [`fallback_note_seed`]) with no loop nonce, so it produces the\n  **same** keep/drop mask every loop pass — a fixed, host-decorrelated\n  pattern.\n- *Per-note `NoteExpression::probability`* is an authored value carried on\n  each individual note; the engine keys its roll on note **identity** +\n  absolute tick + a loop nonce (`note_passes_probability`), so it **re-rolls\n  each loop pass** and needs no graph. Use it for hand-marked per-note odds.\n\nMaking the gate subsume the per-note field would just move the roll into the\ngraph while keeping the field as its data source — no simplification, and it\nloses the \"works without a graph\" property. Keep both.",
      "properties": {
        "probability": {
          "$ref": "#/$defs/NormalizedValue",
          "default": 1.0,
          "description": "Pass probability, 0.0 (block all) .. 1.0 (pass all)."
        },
        "seed": {
          "default": 0,
          "description": "Roll seed — same seed → identical keep/drop decisions.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "Ratchet": {
      "description": "A ratchet / roll (timing node, plan §5.A): subdivides each incoming note into\n`count` fast retriggers spaced `sub_ticks` apart, for as long as the note\nsounds. Like [`NoteDelay`] it is pure look-back — at tick `t` it re-derives\nthe onsets that entered it at `t − m·sub_ticks` and re-emits those still\nsounding — but the dry onset is shortened to one subdivision so the note is\n*replaced* by its stutter rather than layered under it. RT-safe: bounded by\n`count`, [`MAX_NOTE_DELAY_TICKS`], and the buffer cap.",
      "properties": {
        "count": {
          "default": 4,
          "description": "Retriggers per note, including the first (clamped to\n[`MAX_RATCHET_SUBDIVISIONS`]). `1` is a no-op passthrough.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "decay": {
          "$ref": "#/$defs/NormalizedValue",
          "default": 1.0,
          "description": "Per-retrigger velocity multiplier (`1.0` = no decay)."
        },
        "sub_ticks": {
          "$ref": "#/$defs/Duration",
          "default": 120,
          "description": "Ticks between retriggers (the subdivision length)."
        }
      },
      "type": "object"
    },
    "ReturnBus": {
      "description": "Definition of a return bus (effect-send destination) as stored in the song.\n\nThis is the persistent, source-of-truth description — id, name, and fader\n(volume/pan/mute). The engine owns the runtime channel (the effect chain and\naudio buffers) keyed by `id`, and reads this fader live each block, exactly\nas it reads per-track controls (Model C: the song owns channel/routing\ndata). The effect-chain *contents* on a return are engine-side runtime state\nand are not yet persisted here (a follow-up, matching master effects).",
      "properties": {
        "color": {
          "$ref": "#/$defs/TrackColor",
          "default": {
            "b": 255,
            "g": 100,
            "r": 100
          },
          "description": "Display color for the mixer strip."
        },
        "description": {
          "description": "Free-text description / intent (e.g. \"shared plate reverb\"). Empty by\ndefault; readable/writable via MCP and GUI. Never affects audio.",
          "type": "string"
        },
        "id": {
          "$ref": "#/$defs/ReturnBusId",
          "description": "Unique identifier, referenced by [`TrackSend::target`]."
        },
        "mute": {
          "default": false,
          "description": "Mute (excluded from the master mix).",
          "type": "boolean"
        },
        "name": {
          "description": "Display name (e.g. \"Reverb\", \"Delay\").",
          "type": "string"
        },
        "pan": {
          "$ref": "#/$defs/BipolarValue",
          "description": "Output pan (-1.0 = left, 0.0 = centre, 1.0 = right)."
        },
        "sends": {
          "description": "Bus-to-bus send taps from this return into other return busses. Empty by\ndefault; must stay acyclic (see [`super::Song::return_send_would_cycle`]).",
          "items": {
            "$ref": "#/$defs/ReturnSend"
          },
          "type": "array"
        },
        "solo": {
          "default": false,
          "description": "Solo: when any return is soloed, only soloed returns reach the master mix\n(bus-to-bus routing still flows). Independent of track solo.",
          "type": "boolean"
        },
        "volume": {
          "$ref": "#/$defs/NormalizedValue",
          "description": "Output fader level (0.0 = silent, 1.0 = unity)."
        }
      },
      "required": [
        "id",
        "name",
        "volume",
        "pan"
      ],
      "type": "object"
    },
    "ReturnBusEffectsState": {
      "description": "The effect chain on one return bus, in processing order, for persistence.",
      "properties": {
        "effects": {
          "description": "Effects in chain order (each a module type + parameter values).",
          "items": {
            "$ref": "#/$defs/ModuleState"
          },
          "type": "array"
        },
        "id": {
          "description": "Return bus id (matches a `ReturnBus` in the song).",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "id",
        "effects"
      ],
      "type": "object"
    },
    "ReturnBusId": {
      "description": "Unique identifier for a return bus (effect-send destination).\n\nReturn busses are an engine concept (a sub-mix with its own effect chain),\nbut tracks reference them by this id in their send taps, so the id lives in\nthe sequencer alongside the routing data it keys.",
      "format": "uint16",
      "maximum": 65535,
      "minimum": 0,
      "type": "integer"
    },
    "ReturnSend": {
      "description": "A send tap from one return bus into another (bus-to-bus aux routing).\n\nThe source return's post-fader output is summed into the `target` return,\nscaled by `level`. Used for serial/parallel return chains (e.g. a delay\nreturn feeding a reverb return). The routing must stay acyclic — see\n[`super::Song::return_send_would_cycle`].",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Whether the send is active (non-destructive bypass when `false`).",
          "type": "boolean"
        },
        "level": {
          "$ref": "#/$defs/NormalizedValue",
          "description": "Send level (0.0 = no send, 1.0 = unity)."
        },
        "target": {
          "$ref": "#/$defs/ReturnBusId",
          "description": "Destination return bus."
        }
      },
      "required": [
        "target",
        "level"
      ],
      "type": "object"
    },
    "RowCount": {
      "description": "Number of rows in a pattern (1-65535).\n\nClamped to a minimum of 1 — a pattern always has at least one row.",
      "format": "uint16",
      "maximum": 65535,
      "minimum": 0,
      "type": "integer"
    },
    "RowResolution": {
      "description": "Row resolution configuration for pattern view.",
      "properties": {
        "rows": {
          "$ref": "#/$defs/RowCount",
          "description": "Number of rows in this pattern."
        },
        "ticks_per_row": {
          "$ref": "#/$defs/TicksPerRow",
          "description": "Ticks per row."
        }
      },
      "required": [
        "rows",
        "ticks_per_row"
      ],
      "type": "object"
    },
    "ScaleMask": {
      "description": "A 12-bit pitch-class set relative to a root (bit 0 = the root itself).\n\nThe persisted, RT-cheap scale representation: membership is one shift and\nmask, no name lookup. Name → mask mapping for GUI/MCP lives at those\nboundaries (the app crate's harmony tables cannot be a dependency here).",
      "format": "uint16",
      "maximum": 4095,
      "minimum": 0,
      "type": "integer"
    },
    "ScaleQuantize": {
      "additionalProperties": false,
      "description": "Scale-quantize: snap every pitch to the nearest member of `root` + `mask`.\n\nFirst stage of the locked chain so randomized / interval-built notes from\ndownstream generators stay in key. Equidistant candidates prefer the upper\npitch (the melody side of dyads, matching the app-side scale snapper).",
      "properties": {
        "mask": {
          "$ref": "#/$defs/ScaleMask",
          "default": 2741,
          "description": "Scale membership relative to the root."
        },
        "root": {
          "$ref": "#/$defs/PitchClass",
          "default": 0,
          "description": "Tonic pitch class."
        }
      },
      "type": "object"
    },
    "SectionColor": {
      "description": "Persisted RGB color for an arrangement section.",
      "properties": {
        "blue": {
          "description": "Blue channel.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "green": {
          "description": "Green channel.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "red": {
          "description": "Red channel.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "red",
        "green",
        "blue"
      ],
      "type": "object"
    },
    "SectionId": {
      "description": "Unique identifier for an arrangement section marker.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "SectionKind": {
      "description": "Semantic role of an arrangement section.",
      "enum": [
        "intro",
        "verse",
        "pre_chorus",
        "chorus",
        "bridge",
        "break",
        "solo",
        "outro",
        "custom"
      ],
      "type": "string"
    },
    "Semitones": {
      "description": "Pitch offset in semitones.\n\n1 semitone = 100 cents\n12 semitones = 1 octave",
      "format": "float",
      "type": "number"
    },
    "SequencerTrack": {
      "description": "A sequencer track in the song.\nNamed SequencerTrack to distinguish from future AudioTrack.",
      "properties": {
        "color": {
          "$ref": "#/$defs/TrackColor",
          "description": "Track color for UI."
        },
        "description": {
          "default": "",
          "description": "Free-text description capturing intent (e.g. \"kick layer\",\n\"sidechain source\"). Empty by default; readable/writable via MCP and GUI.",
          "type": "string"
        },
        "id": {
          "$ref": "#/$defs/TrackId",
          "description": "Unique identifier."
        },
        "instrument": {
          "$ref": "#/$defs/InstrumentId",
          "default": 0,
          "description": "Instrument this track routes its notes to. Every track has one;\ninstruments may be shared across tracks (intentional layering). Defaults\nto `InstrumentId::new(0)` for a freshly created track until reassigned."
        },
        "mode": {
          "$ref": "#/$defs/TrackMode",
          "description": "Playback mode (polyphonic or mono-voice)."
        },
        "mute": {
          "description": "Muted state.",
          "type": "boolean"
        },
        "name": {
          "description": "Track name.",
          "type": "string"
        },
        "pan": {
          "$ref": "#/$defs/BipolarValue",
          "description": "Panning (type-safe: -1.0 = left, 0.0 = center, 1.0 = right)."
        },
        "sends": {
          "description": "Effect-send taps from this channel to return busses. Empty by default.",
          "items": {
            "$ref": "#/$defs/TrackSend"
          },
          "type": "array"
        },
        "solo": {
          "description": "Solo state.",
          "type": "boolean"
        },
        "volume": {
          "$ref": "#/$defs/NormalizedValue",
          "description": "Volume (type-safe normalized 0.0-1.0)."
        }
      },
      "required": [
        "id",
        "name",
        "volume",
        "pan",
        "mute",
        "solo",
        "color",
        "mode"
      ],
      "type": "object"
    },
    "Song": {
      "description": "A complete song with patterns, tracks, and arrangement.",
      "properties": {
        "arrangement": {
          "items": {
            "$ref": "#/$defs/PatternPlacement"
          },
          "type": "array"
        },
        "author": {
          "description": "Song author.",
          "type": "string"
        },
        "default_tempo": {
          "$ref": "#/$defs/Bpm",
          "description": "Default tempo (BPM)."
        },
        "default_time_signature": {
          "$ref": "#/$defs/TimeSignature",
          "description": "Default time signature."
        },
        "description": {
          "default": "",
          "description": "Free-text description capturing intent (what the song is for, its mood\nor production notes). Empty by default; readable/writable via MCP and GUI.",
          "type": "string"
        },
        "mod_graphs": {
          "description": "Pooled Mod Grid graphs — control-rate modulator graphs referenced by\n[`ModGraphId`]. Global graphs run one always-on instance; Track graphs\nrun one instance per assigned track. The engine (re)builds running\ninstances off the audio thread whenever [`Self::mod_grid_generation`]\nchanges.",
          "items": {
            "$ref": "#/$defs/ModGraph"
          },
          "type": "array"
        },
        "name": {
          "description": "Song name.",
          "type": "string"
        },
        "next_mod_graph_id": {
          "default": 0,
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "next_note_graph_id": {
          "default": 0,
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "next_pattern_id": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "next_return_bus_id": {
          "default": 0,
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "next_section_id": {
          "default": 0,
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "next_track_id": {
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "note_graphs": {
          "description": "Pooled Note Grid graphs — project assets referenced by [`NoteGraphId`].\nA pattern binds one; editing a graph affects every binder. Their derived\n`processing_order` is not serialized; [`Self::rebuild_note_graphs`]\nrecomputes it after load.",
          "items": {
            "$ref": "#/$defs/NoteGraph"
          },
          "type": "array"
        },
        "patterns": {
          "items": {
            "$ref": "#/$defs/Pattern"
          },
          "type": "array"
        },
        "return_busses": {
          "description": "Return busses (effect-send destinations) referenced by `TrackSend`s.\nSource of truth for the busses' fader (read live by the engine).",
          "items": {
            "$ref": "#/$defs/ReturnBus"
          },
          "type": "array"
        },
        "row_resolution": {
          "$ref": "#/$defs/RowResolution",
          "default": {
            "rows": 64,
            "ticks_per_row": 240
          },
          "description": "Row resolution for pattern grid display and quantization."
        },
        "sections": {
          "items": {
            "$ref": "#/$defs/ArrangementSection"
          },
          "type": "array"
        },
        "tempo_changes": {
          "items": {
            "$ref": "#/$defs/TempoChange"
          },
          "type": "array"
        },
        "time_signature_changes": {
          "items": {
            "$ref": "#/$defs/TimeSignatureChange"
          },
          "type": "array"
        },
        "tracks": {
          "description": "Tracks in display order (top → bottom).",
          "items": {
            "$ref": "#/$defs/SequencerTrack"
          },
          "type": "array"
        },
        "transport_loop": {
          "anyOf": [
            {
              "$ref": "#/$defs/LoopRegion"
            },
            {
              "type": "null"
            }
          ],
          "description": "Persisted transport loop region. The runtime loop lives in the sequencer\nengine (which the audio thread owns); this is the save/load carrier only,\nsynced engine→here at save and here→engine at load. `None` = no loop set."
        }
      },
      "required": [
        "name",
        "author",
        "patterns",
        "next_pattern_id",
        "tracks",
        "next_track_id",
        "arrangement",
        "tempo_changes",
        "time_signature_changes",
        "default_tempo",
        "default_time_signature"
      ],
      "type": "object"
    },
    "StealingStrategy": {
      "description": "Strategy for stealing voices when all are busy.",
      "oneOf": [
        {
          "const": "None",
          "description": "Don't steal - ignore new notes when full.",
          "type": "string"
        },
        {
          "const": "Oldest",
          "description": "Steal the oldest active voice.",
          "type": "string"
        },
        {
          "const": "Quietest",
          "description": "Steal the quietest voice (based on envelope level).",
          "type": "string"
        },
        {
          "const": "LowestPriority",
          "description": "Steal the lowest priority voice.",
          "type": "string"
        },
        {
          "const": "SameNote",
          "description": "Steal the same note if playing.",
          "type": "string"
        }
      ]
    },
    "StepLfo": {
      "description": "A step-sequenced control `Value` source: the output is the current step's\nlevel, `step = (tick / step_len) % steps.len()`. Pure tick arithmetic, no\ncross-tick state (plan §5.C).",
      "properties": {
        "step_len": {
          "$ref": "#/$defs/Duration",
          "default": 240,
          "description": "Ticks per step (0 = frozen on the first step)."
        },
        "steps": {
          "default": [
            1.0,
            0.5,
            0.75,
            0.25
          ],
          "description": "Per-step output levels (`0.0..1.0`); cycles when exhausted. Empty = 0.",
          "items": {
            "$ref": "#/$defs/NormalizedValue"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "StrumDirection": {
      "description": "Order in which a strummed chord's tones are struck.",
      "oneOf": [
        {
          "const": "Up",
          "description": "Lowest pitch first (an upstroke).",
          "type": "string"
        },
        {
          "const": "Down",
          "description": "Highest pitch first (a downstroke).",
          "type": "string"
        }
      ]
    },
    "TempoChange": {
      "description": "Tempo change event.",
      "properties": {
        "bpm": {
          "$ref": "#/$defs/Bpm",
          "description": "New tempo in BPM."
        },
        "ramp": {
          "default": false,
          "description": "When `true`, the tempo ramps linearly (in tick space) from `bpm` at\n`tick` toward the *next* change's bpm, reaching it exactly at that\nchange (accelerando / ritardando). With no following change the tempo\nholds constant at `bpm`. When `false` (the default) the tempo steps to\n`bpm` at `tick` and holds until the next change.",
          "type": "boolean"
        },
        "tick": {
          "$ref": "#/$defs/Tick",
          "description": "Position of the tempo change."
        }
      },
      "required": [
        "tick",
        "bpm"
      ],
      "type": "object"
    },
    "Tick": {
      "description": "Absolute position on the song timeline.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "TicksPerRow": {
      "description": "Ticks per row in song tick units (960 PPQN).\n\nControls how many song ticks pass before advancing to the next row.\nDefault: 240 song ticks/row = 4 rows per quarter note (16th note resolution).",
      "format": "uint16",
      "maximum": 65535,
      "minimum": 0,
      "type": "integer"
    },
    "TimeSignature": {
      "description": "Time signature (e.g., 4/4, 3/4, 6/8).",
      "properties": {
        "denominator": {
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "numerator": {
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "numerator",
        "denominator"
      ],
      "type": "object"
    },
    "TimeSignatureChange": {
      "description": "Time signature change event.",
      "properties": {
        "signature": {
          "$ref": "#/$defs/TimeSignature",
          "description": "New time signature."
        },
        "tick": {
          "$ref": "#/$defs/Tick",
          "description": "Position of the time signature change."
        }
      },
      "required": [
        "tick",
        "signature"
      ],
      "type": "object"
    },
    "TrackColor": {
      "description": "Track color for UI display.",
      "properties": {
        "b": {
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "g": {
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "r": {
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "r",
        "g",
        "b"
      ],
      "type": "object"
    },
    "TrackId": {
      "description": "Unique identifier for a track.",
      "format": "uint16",
      "maximum": 65535,
      "minimum": 0,
      "type": "integer"
    },
    "TrackMode": {
      "description": "Track playback mode - determines how notes are allocated to voices.",
      "oneOf": [
        {
          "const": "Polyphonic",
          "description": "Polyphonic mode - notes are allocated from the voice pool dynamically.\nStandard keyboard/MIDI behavior.",
          "type": "string"
        }
      ]
    },
    "TrackParam": {
      "description": "Automatable track parameters.",
      "oneOf": [
        {
          "enum": [
            "Volume",
            "Pan",
            "Mute"
          ],
          "type": "string"
        },
        {
          "const": "Pitch",
          "description": "Continuous track-pitch offset applied to the voices playing on the\ntrack — channel-scoped pitch state (portamento, arpeggio, track\nvibrato) that may begin, change, or stop while a note is held. Lane\nvalues use the bipolar encoding: see [`TRACK_PITCH_RANGE`],\n[`track_pitch_semitones`], and [`track_pitch_normalized`].",
          "type": "string"
        }
      ]
    },
    "TrackSend": {
      "description": "A send tap from a track's channel to a return bus.\n\nThe channel's signal is summed into the target return bus scaled by `level`.\n`pre_fader` selects whether the tap is taken before (`true`) or after\n(`false`, the default) the channel fader/pan — post-fader is the common case\n(the send follows the fader), pre-fader keeps the send constant regardless of\nthe channel level (e.g. a fully-wet cue/monitor send).",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Whether the send is active. A disabled send is kept (with its level and\ntap point) but contributes nothing to the return bus — a non-destructive\nbypass, unlike removing the send. Defaults to `true` so older projects and\nfreshly-created sends are enabled.",
          "type": "boolean"
        },
        "level": {
          "$ref": "#/$defs/NormalizedValue",
          "description": "Send level (0.0 = no send, 1.0 = unity)."
        },
        "pre_fader": {
          "default": false,
          "description": "Tap point: `true` = pre-fader, `false` = post-fader.",
          "type": "boolean"
        },
        "target": {
          "$ref": "#/$defs/ReturnBusId",
          "description": "Destination return bus."
        }
      },
      "required": [
        "target",
        "level"
      ],
      "type": "object"
    },
    "TransportNode": {
      "description": "A grid-native transport source node.",
      "properties": {
        "source": {
          "$ref": "#/$defs/TransportSource",
          "default": "BeatPhase"
        }
      },
      "type": "object"
    },
    "TransportSource": {
      "description": "Which transport-derived signal a [`TransportNode`] emits.",
      "oneOf": [
        {
          "const": "BeatPhase",
          "description": "Ramp 0→1 across each beat.",
          "type": "string"
        },
        {
          "const": "BarPhase",
          "description": "Ramp 0→1 across each bar.",
          "type": "string"
        },
        {
          "const": "Tempo",
          "description": "Current tempo, normalized against a nominal 20..300 BPM span.",
          "type": "string"
        },
        {
          "const": "PositionBeats",
          "description": "Absolute song position in beats (unnormalized).",
          "type": "string"
        }
      ]
    },
    "Velocity": {
      "description": "MIDI velocity (0-127, normalized to 0.0-1.0).\n\nRepresents how hard a note was played.\nStored internally as normalized f32 for smooth processing.",
      "format": "float",
      "type": "number"
    },
    "Vibrato": {
      "description": "Per-note vibrato — taxonomy **primitive 1** (a parametric modulator).\n\nAn additive pitch LFO seeded per note; the engine drives it through the same\nadditive offset path the mod-matrix `OscPitch` destination uses (Phase C4),\nnever the destructive `set_param`. `Copy`/alloc-free for the audio thread.",
      "properties": {
        "delay": {
          "$ref": "#/$defs/Milliseconds",
          "description": "Onset delay before the vibrato ramps in after note start."
        },
        "depth": {
          "$ref": "#/$defs/Semitones",
          "description": "Peak pitch deviation."
        },
        "rate": {
          "$ref": "#/$defs/Hertz",
          "description": "LFO rate."
        },
        "shape": {
          "$ref": "#/$defs/VibratoShape",
          "description": "LFO shape."
        }
      },
      "required": [
        "depth",
        "rate",
        "delay",
        "shape"
      ],
      "type": "object"
    },
    "VibratoShape": {
      "description": "LFO shape for per-note vibrato.\n\nA self-contained sequencer-side enum (like [`GlideInterp`]) so the note model\ndoesn't couple to a `synth_core` LFO param type; the engine maps it onto its\nown LFO in Phase C4.",
      "oneOf": [
        {
          "const": "Sine",
          "description": "Sine (the natural vibrato shape).",
          "type": "string"
        },
        {
          "const": "Triangle",
          "description": "Triangle.",
          "type": "string"
        },
        {
          "const": "Square",
          "description": "Square (trill-like).",
          "type": "string"
        },
        {
          "const": "Saw",
          "description": "Sawtooth (ramp).",
          "type": "string"
        }
      ]
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Top-level project file container.",
  "properties": {
    "active_instrument_id": {
      "description": "Which instrument was active (focused) when saved.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "author": {
      "anyOf": [
        {
          "$ref": "#/$defs/Author"
        },
        {
          "type": "null"
        }
      ],
      "description": "Author / composer of this project."
    },
    "file_type": {
      "const": "project",
      "description": "Always `\"project\"` — distinguishes from patch files.",
      "type": "string"
    },
    "global": {
      "$ref": "#/$defs/GlobalProjectState",
      "default": {
        "glide_time": 0.0,
        "master_volume": 0.800000011920929,
        "octave_offset": 0
      },
      "description": "Global settings not tied to any instrument."
    },
    "instruments": {
      "description": "All instruments with their patches.",
      "items": {
        "$ref": "#/$defs/InstrumentState"
      },
      "type": "array"
    },
    "song": {
      "$ref": "#/$defs/Song",
      "description": "Complete sequencer song (patterns, tracks, arrangement)."
    },
    "version": {
      "description": "Format version (currently `\"1.1\"`).",
      "type": "string"
    }
  },
  "required": [
    "file_type",
    "version",
    "instruments",
    "active_instrument_id",
    "song"
  ],
  "title": "ProjectFile",
  "type": "object"
}
